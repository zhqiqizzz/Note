## 第一轮：先确认项目真实性和你的职责边界

1. **你先整体介绍一下这个 RAG 项目，完整流程是什么？**
2. **你在这个项目里主要负责前端哪几块？哪些是你主导设计的？**
3. **这个项目和普通聊天机器人相比，核心区别是什么？**
4. **你觉得这个项目里最能体现你前端能力的两个点是什么？**

## 第二轮：先问你对 RAG 的理解

5. **什么是 RAG？它和纯大模型直接问答的区别是什么？**
6. **你的系统里 RAG 的完整链路是怎样的，从上传文档到最终回答分别经历了什么？**
7. **为什么要做知识库问答，而不是直接把整篇文档发给模型？**
8. **RAG 场景里你觉得前端的核心价值是什么？**

## 第三轮：文档上传与知识入库

你简历里写了支持 PDF、DOCX、TXT、Markdown 等文档上传和知识入库。

9. **你们支持哪些文档格式？不同格式解析方式有什么区别？**
10. **为什么上传和知识入库要拆成独立流程，而不是“上传成功就等于入库成功”？**
11. **如果用户上传的是大文件，前端怎么保证页面不假死？**
12. **知识入库过程中有哪些状态？前端怎么展示？**
13. **如果文档解析失败、入库失败、部分分块失败，你怎么设计错误反馈？**

追问：
**第一：文件 hash 很耗时的话，你是怎么避免阻塞主线程的？**  
**第二：如果分片上传完了，但服务端合并失败，你前端怎么处理？**

## 第四轮：重点深挖分块策略

你简历里写了：**Web Worker 处理大文本分块，采用按段落、按句子、按固定长度的递归分块策略**。

14. **为什么要分块？不分块会有什么问题？**
15. **为什么采用“段落 -> 句子 -> 固定长度”的递归分块，而不是简单按固定字数切？**
16. **你这个分块策略具体是怎么设计的？**
17. **块大小和重叠长度为什么要可配置？**
18. **如果某一段特别长，超过阈值，你怎么继续切？**
19. **分块过细和过粗分别会带来什么问题？**
20. **为什么分块要放到 Web Worker 里做？主线程直接做有什么问题？**
21. **Worker 和主线程之间是怎么通信的？有没有传输开销问题？**

这块非常高频，面试官会看你是不是真的理解 **RAG 为什么要 chunk**，还是只会背“提高召回效果”。

## 第五轮：问你检索链路和召回理解

22. **向量检索在你这个项目里起什么作用？**
23. **用户提问之后，系统是怎么找到相关知识片段的？**
24. **为什么不直接全文搜索，而要做语义检索？**
25. **检索条数 topK 为什么要可配置？**
26. **如果召回的片段不准，可能是哪些环节出了问题？**
27. **你怎么理解 chunk size、overlap、topK 对最终回答质量的影响？**
28. **如果用户问的问题跨多个文档，你们系统怎么处理？**

## 第六轮：流式对话与 SSE

你简历里写了：**基于 SSE 的流式对话能力，Pinia 统一管理会话状态、消息列表、流式输出内容与中断控制**。

29. **为什么这里选 SSE，而不是 WebSocket？**
30. **SSE 的核心机制是什么？**
31. **流式输出时，前端是怎么一点点拼接消息内容的？**
32. **为什么会有“中断控制”？这个功能怎么实现？**
33. **页面切换后仍可持续接收结果，你是怎么做到的？**
34. **如果 SSE 连接中断了，前端怎么处理？**
35. **流式返回过程中，如何区分“还在生成”还是“真正结束”？**

## 第七轮：问你状态管理设计

36. **为什么这里要用 Pinia 管会话状态，而不是全放组件内部？**
37. **你的 store 大概拆成了哪些状态？**
38. **消息列表、当前流式内容、历史会话、模型参数，这些状态之间怎么解耦？**
39. **如果用户切换会话时上一个回答还在流式返回，你怎么避免状态串台？**
40. **你有没有遇到过流式内容更新过快导致 UI 抖动或重复渲染的问题？**

## 第八轮：Markdown 渲染

你写了：**结合 marked 对模型返回的 Markdown 内容进行结构化渲染**。

41. **为什么要把模型返回内容按 Markdown 渲染，而不是直接展示纯文本？**
42. **marked 在这里解决了什么问题？**
43. **代码块、列表、引用这些富文本在流式场景下有什么特殊问题？**
44. **如果模型返回了不安全内容，比如恶意 HTML，你怎么处理？**
45. **Markdown 流式渲染时，半截代码块或半截表格怎么保证展示不乱？**

## 第九轮：自动滚动与交互体验

你写了：**通过 ResizeObserver 监听消息气泡高度变化，实现长对话自动滚动吸底**。

46. **为什么不用简单的 scrollTop = scrollHeight，而要用 ResizeObserver？**
47. **流式输出过程中，自动滚动最容易遇到什么问题？**
48. **如果用户正在手动往上翻历史消息，这时还要不要强制吸底？**
49. **你是怎么判断“当前应该自动滚动”还是“尊重用户阅读位置”的？**

## 第十轮：上传优化与断点续传

你简历里写了：**固定大小分片 + 文件哈希校验 + 分片索引上传 + 服务端状态记录，实现秒传、断点续传和整体进度展示**。

50. **为什么知识库上传要做分片，而不是直接整文件上传？**
51. **文件哈希是怎么用的？秒传的原理是什么？**
52. **断点续传具体怎么实现？前端如何知道哪些分片已经传过了？**
53. **整体上传进度怎么计算？**
54. **如果某一片上传失败，怎么重试？**
55. **如果用户中途刷新页面，断点续传还能恢复吗？**
56. **为什么这里的上传优化对 RAG 场景尤其有意义？**

## 第十一轮：前端性能优化

你简历里写了：**用 requestAnimationFrame 对高频流式更新做批处理，减少页面抖动**。

57. **为什么流式输出会带来渲染压力？**
58. **你为什么选 requestAnimationFrame，而不是 debounce 或 setTimeout？**
59. **你具体怎么做批处理的？**
60. **优化前后你观察到的效果是什么？**
61. **除了 rAF，你还做过哪些流式渲染优化？**

## 第十二轮：模型参数配置

62. **为什么把 API Key、模型名称、温度、chunk size、overlap、topK 这些做成前端可配置？**
63. **这些参数分别会对回答效果造成什么影响？**
64. **哪些参数更偏“模型生成”，哪些更偏“检索效果”？**
65. **如果用户乱配参数导致效果变差，你怎么做默认值和兜底？**

## 第十三轮：项目复盘

66. **这个项目里你认为最难的点是什么？**
67. **如果让你再做一次，你最想重构哪一部分？**
68. **这个项目最能体现你和普通前端候选人差异化的点是什么？**
69. **如果让你把这个项目继续产品化，你下一步会加什么能力？**
70. **你觉得这个项目里哪些地方是“前端真正做出了价值”，而不是只是调接口？**

---

## 如果我是面试官，我最可能重点追杀的 6 个点

1. **递归分块策略为什么这样设计**
2. **为什么用 Web Worker**
3. **SSE 流式输出和状态管理**
4. **rAF 批处理优化**
5. **断点续传 + 秒传**
6. **RAG 参数如何影响答案质量**