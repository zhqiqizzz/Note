
这个项目可以拆成 **8 个核心业务组件**：

1. **文档上传组件**
2. **文档解析组件**
3. **文本分块组件**
4. **知识入库 / 向量化组件**
5. **知识库管理组件**
6. **检索召回组件**
7. **对话问答组件**
8. **模型参数配置组件**

# 1. 文件上传

## 1. **文档上传组件**

> `app/components/DocumentUpload.vue`

- 负责知识库管理弹窗的整体 UI
- 提供拖拽上传区域
- 提供点击选择文件上传
- 限制可选文件类型：`.pdf / .txt / .md / .docx`
- 上传中展示进度条、上传百分比、已上传大小 / 总大小
- 展示错误提示和通知提示
- 展示已上传文档列表
- 展示每个文档的文件类型、文件大小、向量化状态、文本块数量
- 支持删除文档，删除前会提示“删除文档将同时删除其向量数据”
- 不直接实现上传逻辑，而是通过父组件传入的 `docState.uploadDocument(file)` 触发上传流程

==作用==：源码里可以看到，这个组件主要是 **知识库文档上传和文档列表管理的 UI 层**。它本身不做哈希计算、分片上传、文本分块、向量化，而是负责接收用户选择的文件，然后调用 `props.docState.uploadDocument(file)`。上传进度、上传百分比、上传字节数、错误信息、通知信息、文档列表也都是从 `docState` 中读取。

### 1.1 **文档上传组件的入口位置**

> `app/pages/index.vue`

- 首页中维护 `showDocPanel`
- 点击顶部“知识库”按钮后展示 `DocumentUpload`
- 通过 `:doc-state="docState"` 把文档上传状态传给上传组件
- 使用 `useDocumentsAdvanced()` 创建文档管理状态
- 页面挂载时调用：
    - `docState.loadDocuments()`
    - `docState.initWorker()`
- 页面卸载时调用：
    - `docState.terminateWorker()`

==作用==：`index.vue` 是这个上传面板的挂载入口。源码里 `DocumentUpload` 并不是路由页面，而是首页聊天布局里的一个侧边知识库面板。`index.vue` 创建 `docState = useDocumentsAdvanced()`，然后把它传给 `DocumentUpload`，所以真正的上传逻辑入口在 composable 里。

调用关系是：

```
app/pages/index.vue
   ↓
useDocumentsAdvanced()
   ↓
<DocumentUpload :doc-state="docState" />
```

### 1.2 **文档上传核心逻辑**

> `app/composables/useDocuments.js`

这个文件里有两个 composable：

```
useDocuments()
useDocumentsAdvanced()
```

真正对应你简历里“秒传、断点续传、分片上传、上传进度计算”的是：

> `useDocumentsAdvanced()`

它主要做这些事：

- 维护文档列表 `documents`
- 维护上传状态 `isUploading`
- 维护上传文案 `uploadProgress`
- 维护上传百分比 `uploadPercent`
- 维护已上传大小 `uploadedBytes`
- 维护总文件大小 `totalBytes`
- 维护错误信息 `error`
- 维护提示信息 `notice`
- 计算文件哈希 `computeFileHash(file)`
- 按固定大小切分文件，源码里 `chunkSize = 2 * 1024 * 1024`
- 请求 `/api/upload/check` 判断是否秒传或断点续传
- 跳过服务端已经存在的分片
- 使用 `XMLHttpRequest` 上传分片到 `/api/upload/chunk`
- 上传完成后请求 `/api/upload/merge`
- 合并完成后拿到解析出的文本
- 调用 Web Worker 进行文本分块
- 调用 `/api/embed` 生成向量并入库
- 最后重新加载文档列表

==作用==：`DocumentUpload.vue` 是 UI 层，`useDocumentsAdvanced()` 才是 **文件上传业务编排层**。源码里可以看到，完整上传流程不是一次性 POST 文件，而是：先算 SHA-256 文件哈希，再检查服务端状态，再分片上传缺失部分，最后合并文件、解析文本、分块、向量化。

核心流程可以写成：

```
用户选择文件
   ↓
DocumentUpload.vue 调用 docState.uploadDocument(file)
   ↓
useDocumentsAdvanced.uploadDocument(file)
   ↓
computeFileHash(file)
   ↓
POST /api/upload/check
   ↓
判断是否秒传 / 是否存在已上传分片
   ↓
file.slice() 切分文件
   ↓
POST /api/upload/chunk 上传缺失分片
   ↓
POST /api/upload/merge 合并分片并解析文本
   ↓
Web Worker 文本分块
   ↓
POST /api/embed 生成向量并写入 ChromaDB
   ↓
loadDocuments() 刷新文档列表
```

### 1.3 **上传检查接口**

> `server/api/upload/check.post.js`

- 接收前端传来的文件元信息
- 校验上传元数据是否合法
- 根据 `fileHash` 查询是否已有完整文档
- 如果已有完整文档，返回 `exists: true`
- 如果没有完整文档，则创建或更新上传会话
- 查询服务端已经存在的分片索引
- 计算已上传字节数
- 返回 `uploadedChunks / uploadedBytes / totalChunks`

==作用==：这个接口支撑 **秒传和断点续传**。源码里会先 `findDocumentByHash(body.fileHash)`，如果找到已有文档，就直接返回 `exists: true`；否则通过 `upsertUploadSession` 创建上传会话，并返回已经上传过的分片列表。

也就是：

```
前端算出 fileHash
   ↓
请求 /api/upload/check
   ↓
服务端查是否有同 hash 文档
   ↓
有：秒传
   ↓
没有：返回已上传分片，用于断点续传
```

### 1.4 **分片上传接口**

> `server/api/upload/chunk.post.js`

- 接收 multipart 分片数据
- 读取分片文件字段 `chunk`
- 读取上传元信息：
    - `fileHash`
    - `fileName`
    - `fileSize`
    - `totalChunks`
    - `chunkSize`
    - `chunkIndex`
    - `ext`
- 校验分片索引是否合法
- 校验上传元数据是否合法
- 创建或更新上传会话
- 可选校验 `chunkHash`
- 确保分片目录存在
- 将当前分片写入本地文件系统

==作用==：这个接口是 **单个分片的接收层**。前端每次上传一个 chunk，服务端就按照 `fileHash + chunkIndex` 把它保存成对应的 `.part` 文件。源码里最终是通过 `writeFile(getUploadChunkPath(meta.fileHash, chunkIndex), filePart.data)` 把分片落盘。

### 1.5 **分片合并接口**

> `server/api/upload/merge.post.js`

- 校验上传元数据
- 读取上传会话
- 再次检查是否已有相同 hash 的文档
- 获取已经上传的分片索引
- 检查是否还有缺失分片
- 按 `0 ~ totalChunks - 1` 的顺序读取分片
- 使用 `createWriteStream` 合并成完整文件
- 调用 `parseDocument(fileBuffer, session.fileName)` 提取文本
- 生成文档元信息 `docMeta`
- 写入文档元数据文件
- 删除临时分片目录
- 删除上传会话文件
- 返回文档元信息和解析出的文本

==作用==：这个接口是 **上传阶段和 RAG 入库阶段的分界点**。在它之前，系统处理的是文件分片；在它之后，系统已经拿到了完整文件和解析文本，后续就可以进入文本分块和向量化流程。源码里合并完成后会调用 `parseDocument` 提取文本，并写入 `documents/${documentId}.meta.json`，其中初始状态是 `hasVectors: false`、`chunksCount: 0`。

### 1.6 **上传会话与校验工具**

> `server/utils/uploadSessions.js`

- 定义最大文件大小：`20MB`
- 定义允许的文件类型：`pdf / txt / md / docx`
- 生成上传会话文件路径
- 生成分片目录路径
- 生成单个分片路径
- 校验上传元数据
- 创建分片目录
- 读取已上传分片索引
- 计算已上传字节数
- 读取 / 写入上传会话
- 根据 `fileHash` 查找已完成文档
- 提供服务端 `sha256` 校验方法

==作用==：这个文件是上传模块的 **底层工具层**。`check.post.js`、`chunk.post.js`、`merge.post.js` 都依赖它。比如文件类型和大小不是前端写死判断，而是服务端通过 `validateUploadMeta` 再校验一遍，保证上传安全性和数据一致性。

### 1.7 **向量化接口**

> `server/api/embed.post.js`

- 接收 `documentId` 和 `chunks`
- 读取模型配置和 API Key
- 批量调用 embedding 方法生成向量
- 每批最多处理 25 条 chunk
- 读取文档元信息
- 获取或创建 Chroma Collection
- 将：
    - chunk id
    - embedding
    - metadata
    - 原始 chunk 文本  
        写入 ChromaDB
- 同步写入本地 `vectors/${documentId}.json`
- 更新文档元信息：
    - `hasVectors = true`
    - `chunksCount = chunks.length`
    - `updatedAt = 当前时间`

==作用==：这个接口不是文件上传本身，但它是上传流程的最后一步。`useDocumentsAdvanced()` 在文件合并、文本解析、Web Worker 分块之后，会调用 `/api/embed`，把文本块转成向量并存入 ChromaDB。源码里可以看到它使用 `getEmbeddings(batch, settings)` 批量生成向量，然后 `collection.add(...)` 写入 ChromaDB。

### 面试时回答

> 首页入口在 `app/pages/index.vue`，里面通过 `useDocumentsAdvanced()` 创建文档状态，并把它作为 `doc-state` 传给 `DocumentUpload`。真正的上传逻辑在 `app/composables/useDocuments.js`，里面会先计算文件 SHA-256，然后请求 `/api/upload/check` 判断是否秒传或断点续传。如果需要上传，就按 2MB 固定大小切片，用 XHR 上传到 `/api/upload/chunk`，并通过 `xhr.upload.onprogress` 计算进度。所有分片上传完成后，再请求 `/api/upload/merge`，服务端会检查分片完整性、按顺序合并文件、解析文本并返回。前端拿到文本后，再用 Web Worker 分块，最后调用 `/api/embed` 生成向量并写入 ChromaDB。

### 模块完整流程

你这个上传模块，前端页面组件其实主要有两个：

```
app/pages/index.vue
  ↓ 创建 docState，并控制知识库面板显示
app/components/DocumentUpload.vue
  ↓ 接收 docState，负责上传 UI 和文档列表 UI
app/composables/useDocuments.js
  ↓ 真正执行上传、分片、合并、分块、向量化
server/api/upload/check.post.js
server/api/upload/chunk.post.js
server/api/upload/merge.post.js
server/api/embed.post.js
```

整体关系可以先这样记：

```
用户点击“知识库”
  ↓
app/pages/index.vue 打开 DocumentUpload 面板
  ↓
DocumentUpload.vue 选择文件 / 拖拽文件
  ↓
调用 docState.uploadDocument(file)
  ↓
useDocumentsAdvanced.uploadDocument(file)
  ↓
计算文件 hash
  ↓
/api/upload/check 检查秒传 / 断点续传
  ↓
/api/upload/chunk 上传缺失分片
  ↓
/api/upload/merge 合并分片并解析文本
  ↓
Web Worker 分块
  ↓
/api/embed 生成向量并写入 ChromaDB
  ↓
loadDocuments() 刷新知识库列表
```

#### 上传模块挂载入口

>文件：`app/pages/index.vue`

它是整个 RAG 聊天页面的入口，同时也是知识库上传面板的挂载位置。这个页面本身不负责上传细节，而是负责：

- 创建文档状态对象 `docState`
- 控制知识库面板是否显示
- 把 `docState` 传给 `DocumentUpload`
- 页面挂载时加载文档列表
- 页面挂载时初始化 Web Worker
- 页面卸载时销毁 Web Worker

源码里可以看到，首页通过 `useDocumentsAdvanced()` 创建文档管理状态，然后把它传给 `<DocumentUpload :doc-state="docState" />`。

##### 1. template 结构

核心 template 可以抽象成这样：

```vue
<div class="chat-layout">
  <ChatSidebar
    :documents-count="docState.documents.value.length"
    @open-docs="showDocPanel = true"
  />

  <div class="chat-main">
    <header class="chat-header">
      <button
        class="btn-header"
        :class="{ active: showDocPanel }"
        @click="showDocPanel = !showDocPanel"
      >
        知识库
        <span v-if="docState.documents.value.length">
          {{ docState.documents.value.length }}
        </span>
      </button>

      <NuxtLink to="/settings">
        设置
      </NuxtLink>
    </header>

    <ChatWindow />
    <ChatInput />
  </div>

  <DocumentUpload
    v-if="showDocPanel"
    @close="showDocPanel = false"
    :doc-state="docState"
  />
</div>
```

它做了三件事：

1. 左侧 `ChatSidebar` 展示文档数量，并提供打开知识库面板的入口。
2. 顶部 header 里有“知识库”按钮，点击后切换 `showDocPanel`。
3. 当 `showDocPanel = true` 时，渲染 `DocumentUpload` 组件，并把 `docState` 传进去。

这里的关键点是：

```vue
<DocumentUpload
  v-if="showDocPanel"
  @close="showDocPanel = false"
  :doc-state="docState"
/>
```

也就是说，`DocumentUpload.vue` 自己不创建上传状态，而是完全接收父组件传入的 `docState`。

##### 2. 核心变量

###### `chatStore`

```
const chatStore = useChatStore()
```

这个是聊天模块状态，和上传模块不是强绑定，但页面里会同时管理聊天状态和知识库状态。

它主要负责：

- 当前会话标题
- 是否正在流式生成
- 来源片段展示
- 会话列表加载

上传模块里只需要知道：它和文档模块并列存在于首页。
###### `docState`

```js
const docState = useDocumentsAdvanced()
```

这是上传模块最重要的变量。

`docState` 不是普通对象，而是 `useDocumentsAdvanced()` 返回的一整套文档管理状态和方法。

里面包括：

```js
{
  documents,
  isUploading,
  uploadProgress,
  uploadPercent,
  uploadedBytes,
  totalBytes,
  error,
  notice,
  loadDocuments,
  uploadDocument,
  deleteDocument,
  initWorker,
  terminateWorker
}
```

所以 `index.vue` 的职责不是自己处理上传，而是创建这个状态对象，然后交给子组件使用。

###### `showDocPanel`

```js
const showDocPanel = ref(false)
```

它控制知识库面板是否显示。

```
false：不显示上传面板
true：显示上传面板
```

这个变量对应 template 里的：

```vue
<DocumentUpload v-if="showDocPanel" />
```

以及按钮上的：

```vue
@click="showDocPanel = !showDocPanel"
```

##### 3. 核心函数 / 生命周期

==`onMounted`==

```js
onMounted(async () => {
  await chatStore.loadConversations()
  await docState.loadDocuments()
  docState.initWorker()
})
```

它做了三件事：

1. 加载历史会话。
2. 加载知识库文档列表。
3. 初始化文本分块用的 Web Worker。

对上传模块来说，关键是后两步：

```js
await docState.loadDocuments()
docState.initWorker()
```

这表示首页一进入，就会先把已有文档列表拉出来，这样知识库按钮上的数量和上传面板里的文档列表都是最新的。

同时提前初始化 Worker，后续上传文档并解析文本后，就可以直接把大文本交给 Worker 分块。

==`onUnmounted`==

```js
onUnmounted(() => {  docState.terminateWorker()})
```

页面销毁时关闭 Worker。

这个设计是合理的，因为 Worker 是独立线程，如果页面离开后不销毁，可能会造成资源占用。

##### 4. 这个页面在上传链路中的作用

可以这样总结：

```
index.vue 不负责具体上传
index.vue 只负责创建 docState
index.vue 控制 DocumentUpload 是否显示
index.vue 把 docState 传给 DocumentUpload
index.vue 在生命周期里初始化和销毁 Worker
```

#### 知识库上传面板组件

>文件：`app/components/DocumentUpload.vue`

它负责知识库上传面板的 UI，包括：

- 面板弹层
- 上传区域
- 拖拽上传
- 点击选择文件
- 上传进度展示
- 错误提示
- 通知提示
- 文档列表展示
- 删除文档入口

源码里 `DocumentUpload.vue` 接收 `docState` 作为必传 props，并通过 `docState.uploadDocument(file)` 发起上传。它的文件选择框限制了 `.pdf,.txt,.md,.docx`，上传中会展示 `uploadProgress / uploadPercent / uploadedBytes / totalBytes`，文档列表则读取 `docState.documents.value`。
##### 1. template 结构

这个组件的 template 可以分成五块：

```
DocumentUpload.vue
  ├─ 面板遮罩层 doc-panel-overlay
  ├─ 面板头部 panel-header
  ├─ 上传区域 upload-zone
  ├─ 错误 / 通知提示
  └─ 文档列表 doc-list
```

###### 1.1 面板遮罩层

```vue
<div class="doc-panel-overlay" @click.self="$emit('close')">
  <div class="doc-panel">
    ...
  </div>
</div>
```

它做了两件事：

1. ==外层遮罩==负责覆盖页面。
2. 点击遮罩自身时触发 `close` 事件，通知父组件关闭面板。

这里用了：

```vue
@click.self="$emit('close')"
```

`.self` 的作用是==只有点击遮罩本身才关闭==，点击内部面板不会冒泡导致误关闭。

###### 1.2 面板头部

```vue
<div class="panel-header">
  <h2>知识库管理</h2>
  <button class="btn-icon" @click="$emit('close')">
    关闭图标
  </button>
</div>
```

它负责展示标题和关闭按钮。

这里的关闭逻辑仍然是：

```js
$emit('close')
```

父组件 `index.vue` 收到后执行：

```js
showDocPanel = false
```

###### 1.3 上传区域

```vue
<div
  class="upload-zone"
  :class="{ dragging: isDragging, uploading: docState.isUploading.value }"
  @dragover.prevent="isDragging = true"
  @dragleave="isDragging = false"
  @drop.prevent="handleDrop"
  @click="triggerFileInput"
>
  <input
    ref="fileInputRef"
    type="file"
    accept=".pdf,.txt,.md,.docx"
    class="file-input"
    @change="handleFileSelect"
  />

  <div v-if="docState.isUploading.value" class="upload-progress">
    上传进度
  </div>

  <div v-else class="upload-prompt">
    上传提示
  </div>
</div>
```

这块是上传组件的核心 UI。

它做了几件事：

1. 监听拖拽进入：`@dragover.prevent`
2. 监听拖拽离开：`@dragleave`
3. 监听文件放下：`@drop.prevent`
4. 点击上传区域时触发隐藏的 file input
5. 根据 `docState.isUploading.value` 切换上传进度和上传提示

其中：

```vue
accept=".pdf,.txt,.md,.docx"
```

表示前端文件选择器只允许选择这些类型。

但注意，这只是浏览器层面的限制，真正安全校验还在服务端 `validateUploadMeta` 里做。

###### 1.4 上传进度区域

```vue
<div v-if="docState.isUploading.value" class="upload-progress">
  <span class="spinner"></span>

  <p>{{ docState.uploadProgress.value }}</p>

  <div class="progress-track">
    <div
      class="progress-fill"
      :style="{ width: `${docState.uploadPercent.value || 0}%` }"
    ></div>
  </div>

  <p class="upload-stats">
    {{ formatSize(docState.uploadedBytes.value) }}
    /
    {{ formatSize(docState.totalBytes.value) }}
    ({{ docState.uploadPercent.value || 0 }}%)
  </p>
</div>
```

它展示四类信息：

1. 当前状态文案：`uploadProgress`
2. 当前上传百分比：`uploadPercent`
3. 已上传大小：`uploadedBytes`
4. 文件总大小：`totalBytes`

比如上传过程中会显示：

```
正在上传分片 3/8...6.0 MB / 16.0 MB (38%)
```

这部分对应 `useDocumentsAdvanced()` 里的 `updateProgressState()`。

###### 1.5 上传提示区域

```vue
<div v-else class="upload-prompt">
  <svg>上传图标</svg>
  <p>拖拽文件到此处或<span class="link">点击选择</span></p>
  <p class="upload-hint">支持 PDF、DOCX、TXT、MD (最大 20MB)</p>
</div>
```

当没有上传任务时，显示上传提示。

它告诉用户：

```
支持 PDF、DOCX、TXT、MD
最大 20MB
```

这里和服务端保持一致，服务端 `uploadSessions.js` 里也限制了最大 20MB 和允许类型。

###### 1.6 错误和通知提示

```vue
<div v-if="docState.error.value" class="alert alert-error">
  {{ docState.error.value }}
</div>

<div v-if="docState.notice.value" class="alert alert-info">
  {{ docState.notice.value }}
</div>
```

这两个区域分别展示：

```
error：上传失败、解析失败、分块失败、向量化失败
notice：秒传成功、跳过重复上传等提示
```

比如秒传时，后端返回文件已存在，前端会设置：

```
notice.value = checkResult.message || '文件已存在，已跳过重复上传'
```

###### 1.7 文档列表

```vue
<div class="doc-list">
  <h3 class="list-title">
    已上传文档
    <span class="count">
      {{ docState.documents.value.length }}
    </span>
  </h3>

  <div v-if="!docState.documents.value.length" class="doc-empty">
    <p>暂无文档，上传后即可开始问答</p>
  </div>

  <TransitionGroup name="list" tag="div">
    <div
      v-for="doc in docState.documents.value"
      :key="doc.id"
      class="doc-item"
    >
      ...
    </div>
  </TransitionGroup>
</div>
```

它负责展示已经上传过的文档。

每个文档项展示：

```vue
<div class="doc-icon" :class="`doc-${doc.ext}`">
  {{ doc.ext.toUpperCase() }}
</div>

<div class="doc-name">
  {{ doc.name }}
</div>

<div class="doc-meta">
  <span>{{ formatSize(doc.size) }}</span>

  <span v-if="doc.hasVectors" class="doc-status done">
    ✓ {{ doc.chunksCount }} 个块
  </span>

  <span v-else class="doc-status pending">
    待向量化
  </span>
</div>
```

它会根据 `doc.hasVectors` 判断当前文档是否已经完成向量化：

```
hasVectors = true   → 展示 “✓ n 个块”
hasVectors = false  → 展示 “待向量化”
```

这其实就把后端入库状态反馈到了 UI 上。

##### 2. 核心变量

==`props.docState`==

```js
const props = defineProps({
  docState: { type: Object, required: true }
})
```

这是整个组件最核心的数据来源。

`DocumentUpload.vue` 不自己维护上传状态，而是全部从 `docState` 里读。

常用字段包括：

```js
docState.documents
docState.isUploading
docState.uploadProgress
docState.uploadPercent
docState.uploadedBytes
docState.totalBytes
docState.error
docState.notice
docState.uploadDocument
docState.deleteDocument
```

这种写法的好处是：

```
UI 组件只负责展示和交互
上传逻辑全部收敛到 composable
父组件负责创建状态
子组件负责消费状态
```

==`isDragging`==

```js
const isDragging = ref(false)
```

它表示当前是否有文件拖拽到上传区域上方。

对应 template 里的动态 class：

```vue
:class="{ dragging: isDragging, uploading: docState.isUploading.value }"
```

当用户拖拽文件进入区域时：

```js
isDragging = true
```

离开或者放下时：

```js
isDragging = false
```

它主要用于控制上传区域的视觉反馈。

==`fileInputRef`==

```js
const fileInputRef = ref(null)
```

它指向隐藏的文件选择 input。

template 里：

```vue
<input
  ref="fileInputRef"
  type="file"
  class="file-input"
/>
```

因为 input 被隐藏了，所以点击上传区域时，要通过 JS 主动触发：

```js
fileInputRef.value?.click()
```

这样用户体验上就是“点击整个上传区域都可以选择文件”。

##### 3. 核心函数

==`triggerFileInput`==

```js
function triggerFileInput() {
  if (!props.docState.isUploading.value) {
    fileInputRef.value?.click()
  }
}
```

它的作用是点击上传区域时，打开文件选择窗口。

这里有一个细节：

```js
if (!props.docState.isUploading.value)
```

如果当前正在上传，就不允许再次打开文件选择器。

这样可以避免同一时间重复触发多个上传任务。

==`handleFileSelect`==

```js
async function handleFileSelect(e) {
  const file = e.target.files?.[0]
  if (file) {
    await props.docState.uploadDocument(file)
    e.target.value = ''
  }
}
```

它处理点击选择文件后的逻辑。

流程是：

```
用户点击上传区域
  ↓
触发隐藏 input
  ↓
用户选择文件
  ↓
handleFileSelect 拿到 file
  ↓
调用 docState.uploadDocument(file)
  ↓
清空 input value
```

这里清空：

```js
e.target.value = ''
```

是一个很重要的小细节。

如果不清空，用户连续选择同一个文件时，`change` 事件可能不会再次触发。

==`handleDrop`==

```js
async function handleDrop(e) {
  isDragging.value = false
  const file = e.dataTransfer?.files?.[0]
  if (file) {
    await props.docState.uploadDocument(file)
  }
}
```

它处理拖拽上传。

流程是：

```
用户拖拽文件到上传区域
  ↓
drop 事件触发
  ↓
从 e.dataTransfer.files[0] 取出文件
  ↓
调用 docState.uploadDocument(file)
```

它和 `handleFileSelect` 最终都会走同一个上传入口：

```js
props.docState.uploadDocument(file)
```

也就是说：

```
点击上传
拖拽上传
   ↓
统一进入 uploadDocument(file)
```

这保证了两种交互方式复用同一套上传逻辑。

==`confirmDeleteDoc`==

```js
function confirmDeleteDoc(id) {
  if (window.confirm('删除文档将同时删除其向量数据，确定吗？')) {
    props.docState.deleteDocument(id)
  }
}
```

它负责删除文档前的确认。

这里的提示文案很关键：

```
删除文档将同时删除其向量数据
```

因为 RAG 系统里删除文档不是只删列表记录，还要删除对应向量数据，否则用户删除文档后，聊天检索时仍然可能召回旧内容。

==`formatSize`==

```js
function formatSize(bytes) {
  if (!bytes) return '0 B'
  const units = ['B', 'KB', 'MB', 'GB']
  let i = 0
  let size = bytes

  while (size >= 1024 && i < units.length - 1) {
    size /= 1024
    i++
  }

  return `${size.toFixed(1)} ${units[i]}`
}
```

它负责把字节数格式化成人类可读的单位。

比如：

```
1024      → 1.0 KB
1048576   → 1.0 MB
```

这个函数在两个地方用到：

```
上传进度：uploadedBytes / totalBytes
文档列表：doc.size
```

##### 4. 这个组件在上传链路中的作用

可以这样总结：

```
DocumentUpload.vue 是上传 UI 层
它不做哈希计算
它不做分片切割
它不直接请求上传接口
它只负责：
  1. 选择文件
  2. 拖拽文件
  3. 调用 uploadDocument(file)
  4. 展示上传状态
  5. 展示文档列表
  6. 调用 deleteDocument(id)
```

面试里可以这样说：

> `DocumentUpload.vue` 是知识库上传面板组件，它负责上传区域、拖拽上传、点击选择文件、上传进度展示、错误提示、通知提示和文档列表展示。它本身不处理分片上传，而是通过父组件传入的 `docState` 调用 `uploadDocument(file)`。这样做可以把 UI 和业务逻辑拆开，组件只关心用户交互和状态展示，具体上传流程交给 `useDocumentsAdvanced()` 统一编排。


#### 上传业务编排模块

>文件：`app/composables/useDocuments.js`

这个文件不是页面组件，但它是上传模块的核心逻辑层。

源码里有两个 composable：

```js
useDocuments()
useDocumentsAdvanced()
```

其中你项目当前首页用的是：

```js
const docState = useDocumentsAdvanced()
```

`useDocumentsAdvanced()` 才是支持分片上传、文件 hash、秒传、断点续传、上传进度、Web Worker 分块、向量化入库的完整版本。

##### 1. 核心变量

==`documents`==

```
const documents = ref([])
```

保存知识库文档列表。

文档项大概包含：

```js
{
  id,
  name,
  ext,
  size,
  hasVectors,
  chunksCount,
  createdAt
}
```

它会被 `DocumentUpload.vue` 用来渲染文档列表：

```vue
v-for="doc in docState.documents.value"
```

==`isUploading`==

```js
const isUploading = ref(false)
```

表示当前是否正在上传。

它影响两个地方：

1. `DocumentUpload.vue` 是否显示上传进度。
2. `triggerFileInput()` 是否允许再次选择文件。

==`uploadProgress`==

```js
const uploadProgress = ref('')
```

保存当前上传阶段的文案。

比如：

```
正在准备上传...
正在上传文件...
正在上传分片 1/5...
正在合并文件并提取文本...
正在分块处理...
正在生成向量...
上传完成
```

它直接显示在上传面板里：

```vue
{{ docState.uploadProgress.value }}
```

==`uploadPercent`==

```js
const uploadPercent = ref(0)
```

保存上传百分比。

它控制进度条宽度：

```vue
:style="{ width: `${docState.uploadPercent.value || 0}%` }"
```

==`uploadedBytes / totalBytes`==

```js
const uploadedBytes = ref(0)
const totalBytes = ref(0)
```

用于展示：

```
已上传大小 / 总大小
```

比如：

```
4.0 MB / 12.0 MB (33%)
```

==`error`==

```js
const error = ref(null)
```

保存错误信息。

可能来自：

```
上传校验失败
文件太大
文件类型不支持
分片上传失败
合并文件失败
文本为空
分块失败
向量化失败
```

==`notice`==

```js
const notice = ref('')
```

保存普通提示信息。

主要用于：

```
秒传完成
文件已存在，跳过重复上传
检测到已有分片，继续续传
```

==`worker`==

```js
let worker = null
```

保存 Web Worker 实例。

它用于文本分块：

```js
worker = new Worker('/workers/textProcessor.worker.js')
```

==`chunkSize`==

```js
const chunkSize = 2 * 1024 * 1024
```

这是文件上传分片大小。

也就是说，大文件会按 2MB 切一片。

注意这里的 `chunkSize` 是**文件分片大小**，不是文本分块大小。

项目里其实有两个 chunk 概念：

```
文件上传 chunk：2MB 一个，用于断点续传
文本语义 chunk：比如 500 字一个，用于 RAG 检索
```

这两个不要混淆。

##### 2. 核心函数

==`loadDocuments`==

```js
async function loadDocuments() {
  try {
    documents.value = await $fetch('/api/documents')
  } catch (err) {
    console.error('加载文档列表失败:', err)
  }
}
```

它负责加载已有文档列表。

调用位置有两个：

```
页面初始化时
上传完成后
```

流程是：

```
请求 /api/documents
  ↓
服务端读取 documents 目录下的 meta 文件
  ↓
检查 vectors 文件判断是否向量化
  ↓
返回文档列表
  ↓
赋值给 documents
```

后端 `/api/documents` 会读取文档 meta，并检查对应 vectors 文件，从而补全 `hasVectors` 和 `chunksCount`。

==`initWorker`==

```js
function initWorker() {
  if (typeof Worker !== 'undefined' && !worker) {
    worker = new Worker('/workers/textProcessor.worker.js')
  }
}
```

它初始化文本分块 Worker。

作用是：

```
创建一个后台线程
让大文本分块不要阻塞主线程
```

如果没有 Worker，那么大文档分块时可能卡住 UI。

==`terminateWorker`==

```js
function terminateWorker() {
  if (worker) {
    worker.terminate()
    worker = null
  }
}
```

它销毁 Worker。

调用位置：

```
app/pages/index.vue 的 onUnmounted
```

作用是页面离开后释放资源。

==`uploadDocument`==

这是整个上传模块最核心的函数。

它的完整流程可以拆成 9 步。

**第一步：初始化上传状态**

```js
isUploading.value = true
uploadProgress.value = '正在准备上传...'
uploadPercent.value = 0
uploadedBytes.value = 0
totalBytes.value = file.size || 0
error.value = null
notice.value = ''
```

作用是重置上传状态。

用户一选择文件，就先把 UI 切到上传中。

**第二步：计算文件 hash**

```js
const fileHash = await computeFileHash(file)
const ext = getFileExt(file.name)
const totalChunks = Math.ceil(file.size / chunkSize)
```

这里做了三件事：

1. 用文件内容计算 SHA-256。
2. 从文件名里取后缀。
3. 根据文件大小和 2MB 分片大小计算分片总数。

比如一个 9MB 文件：

```js
chunkSize = 2MB
totalChunks = Math.ceil(9 / 2) = 5
```

`fileHash` 的作用是标识文件内容。

同一个文件只要内容一样，即使文件名不同，hash 也应该一样。

**第三步：检查秒传 / 断点续传**

```js
const checkResult = await $fetch('/api/upload/check', {
  method: 'POST',
  body: {
    fileName: file.name,
    fileSize: file.size,
    fileHash,
    ext,
    chunkSize,
    totalChunks
  }
})
```

这一步会把文件元数据发给服务端。

服务端会判断：

```
这个 hash 的完整文档是否已经存在？
这个 hash 是否有上传过一部分分片？
```

如果已经存在完整文档：

```js
if (checkResult.exists) {
  uploadedBytes.value = file.size
  totalBytes.value = file.size
  uploadPercent.value = 100
  uploadProgress.value = '秒传完成，已复用现有文件'
  notice.value = checkResult.message || '文件已存在，已跳过重复上传'
  await loadDocuments()
  return true
}
```

这就是秒传。

所谓秒传，不是真的重新上传文件，而是服务端发现这个文件已经入库过了，前端直接复用已有结果。

**第四步：处理已上传分片**

```js
const uploadedChunkSet = new Set(checkResult.uploadedChunks || [])
let confirmedBytes = checkResult.uploadedBytes || 0
```

服务端会返回已经上传过的分片索引。

比如：

```js
uploadedChunks = [0, 1, 2]
```

说明前 3 个分片之前已经上传成功。

前端后面循环上传时会跳过这些分片：

```js
if (uploadedChunkSet.has(index)) continue
```

这就是断点续传。

**第五步：循环切片并上传缺失分片**

```js
for (let index = 0; index < totalChunks; index++) {
  if (uploadedChunkSet.has(index)) continue

  const start = index * chunkSize
  const end = Math.min(start + chunkSize, file.size)
  const chunk = file.slice(start, end)

  await uploadChunkWithProgress({
    file,
    chunk,
    chunkIndex: index,
    totalChunks,
    fileHash,
    ext,
    chunkSize,
    onProgress: (loaded) => {
      updateProgressState(
        confirmedBytes + loaded,
        file.size,
        `正在上传分片 ${index + 1}/${totalChunks}...`
      )
    }
  })

  confirmedBytes += chunk.size
}
```

这段逻辑是分片上传的核心。

它做了几件事：

1. 遍历所有分片索引。
2. 如果该分片已经存在，就跳过。
3. 用 `file.slice(start, end)` 切出当前分片。
4. 调用 `uploadChunkWithProgress()` 上传分片。
5. 通过 `onProgress` 更新总进度。
6. 当前分片上传完后，把 `confirmedBytes` 增加。

这里的设计点是：

```
不是每个分片独立展示进度
而是把“已确认上传字节数 + 当前分片 loaded”合成整体进度
```

所以 UI 上展示的是整个文件的上传进度。

**第六步：请求服务端合并文件**

```js
const result = await $fetch('/api/upload/merge', {
  method: 'POST',
  body: {
    fileName: file.name,
    fileSize: file.size,
    fileHash,
    ext,
    chunkSize,
    totalChunks
  }
})
```

所有分片上传完成后，前端通知服务端合并。

服务端会：

```
检查分片是否齐全
按 chunkIndex 顺序读取分片
写入完整文件
解析文档文本
生成文档 meta
删除临时分片
返回 text
```

也就是说，`merge` 接口返回的不只是“合并成功”，它还会返回解析后的文本。

**第七步：校验解析文本**

```js
if (!result.text || !result.text.trim()) {
  throw new Error('文件中未提取到有效文本')
}
```

如果 PDF、DOCX、TXT、MD 里没有提取出文本，就终止流程。

这个判断很重要，因为后续的文本分块和向量化都依赖 `text`。

**第八步：Web Worker 文本分块**

```js
const settings = await $fetch('/api/settings')

const chunks = await chunkTextWithWorker(result.text, {
  chunkSize: settings.chunkSize || 500,
  chunkOverlap: settings.chunkOverlap || 50
})
```

这一步进入 RAG 的文本处理阶段。

前面是文件上传 chunk。

这里是知识库文本 chunk。

它会先请求模型配置里的：

```
chunkSize
chunkOverlap
```

然后交给 Worker 分块。

**第九步：调用向量化接口**

```js
await $fetch('/api/embed', {
  method: 'POST',
  body: { documentId: result.id, chunks }
})
```

这一步把文本块送到服务端生成 embedding。

服务端 `/api/embed` 会批量生成向量，并写入 ChromaDB，同时更新文档 meta 的 `hasVectors` 和 `chunksCount`。

上传完成后：

```js
uploadProgress.value = '上传完成'
await loadDocuments()
```

重新拉取文档列表，UI 中就能看到：

```
✓ n 个块
```

==`chunkTextWithWorker`==

```js
function chunkTextWithWorker(text, options) {
  return new Promise((resolve, reject) => {
    initWorker()

    if (!worker) {
      resolve(fallbackChunkAdvanced(text, options.chunkSize))
      return
    }

    worker.onmessage = ({ data }) => {
      if (data.type === 'chunks') {
        resolve(data.payload)
      } else if (data.type === 'error') {
        reject(new Error(data.payload))
      }
    }

    worker.postMessage({
      type: 'chunk',
      payload: {
        text,
        chunkSize: options.chunkSize,
        chunkOverlap: options.chunkOverlap
      }
    })
  })
}
```

它负责把文本分块任务交给 Worker。

流程是：

```
主线程调用 chunkTextWithWorker
  ↓
初始化 Worker
  ↓
向 Worker postMessage
  ↓
Worker 执行 recursiveChunk
  ↓
Worker 返回 chunks
  ↓
主线程 resolve(chunks)
```

它还做了降级处理：

```js
if (!worker) {
  resolve(fallbackChunkAdvanced(text, options.chunkSize))
}
```

也就是说，如果 Worker 不可用，就退回主线程简单分块。

==`fallbackChunkAdvanced`==

```js
function fallbackChunkAdvanced(text, size = 500) {
  const chunks = []

  for (let i = 0; i < text.length; i += size) {
    const chunk = text.slice(i, i + size).trim()
    if (chunk) chunks.push(chunk)
  }

  return chunks
}
```

这是 Worker 不可用时的降级方案。

它比较简单，就是固定长度切分。

缺点是语义不如 Worker 的递归分块完整。

==`deleteDocument`==

```js
async function deleteDocument(id) {
  try {
    await $fetch(`/api/documents/${id}`, { method: 'DELETE' })
    documents.value = documents.value.filter(doc => doc.id !== id)
  } catch (err) {
    error.value = err.data?.message || err.message || '删除失败'
  }
}
```

它负责删除文档。

流程是：

```
调用 DELETE /api/documents/:id
  ↓
服务端删除文档和向量数据
  ↓
前端从 documents 中过滤掉该文档
```

==`updateProgressState`==

```js
function updateProgressState(currentBytes, fileSize, message) {
  uploadProgress.value = message
  uploadedBytes.value = Math.min(currentBytes, fileSize)
  totalBytes.value = fileSize
  uploadPercent.value = fileSize
    ? Math.round((uploadedBytes.value / fileSize) * 100)
    : 0
}
```

它是上传进度的统一更新函数。

每个分片上传时，都会调用它。

它统一维护四个状态：

```js
uploadProgress
uploadedBytes
totalBytes
uploadPercent
```

这样组件里只需要展示状态，不需要关心进度怎么算。

##### 3. 这个模块在上传链路中的作用

可以总结成：

```
useDocumentsAdvanced 是上传业务编排层
DocumentUpload.vue 负责 UI
server/api 负责实际文件处理和向量化
useDocumentsAdvanced 负责把这些步骤串起来
```

面试里可以这样说：

> `useDocumentsAdvanced()` 是上传模块的核心逻辑。它先维护上传状态和文档列表，然后在 `uploadDocument(file)` 中完成完整的上传编排：计算文件 SHA-256，调用 `/api/upload/check` 判断秒传或断点续传，按 2MB 固定大小切片，用 XHR 上传缺失分片，上传完成后调用 `/api/upload/merge` 合并并解析文本，然后通过 Web Worker 按配置进行文本分块，最后调用 `/api/embed` 生成向量并刷新文档列表。

#### 文本分块 Worker

>文件：`public/workers/textProcessor.worker.js`

这个文件负责上传后的大文本分块。

它运行在 Worker 线程里，不阻塞页面主线程。源码里它通过 `self.onmessage` 接收主线程任务，任务类型是 `chunk` 时，会调用 `recursiveChunk` 进行分块，然后通过 `self.postMessage` 把 chunks 返回给主线程。

##### 1. 核心入口

```js
self.onmessage = function ({ data }) {
  const { type, payload } = data

  switch (type) {
    case 'chunk':
      const chunks = recursiveChunk(payload.text, {
        chunkSize: payload.chunkSize || 500,
        chunkOverlap: payload.chunkOverlap || 50
      })
      self.postMessage({ type: 'chunks', payload: chunks })
      break

    default:
      self.postMessage({ type: 'error', payload: '未知的任务类型' })
  }
}
```

它做了三件事：

1. 接收主线程传来的消息。
2. 如果任务类型是 `chunk`，就执行文本分块。
3. 分块完成后，把结果发回主线程。

##### 2. 核心函数：

==`recursiveChunk`==

```js
function recursiveChunk(text, options) {
  const { chunkSize, chunkOverlap } = options

  const cleanedText = text.replace(/\n{3,}/g, '\n\n').trim()

  const paragraphs = cleanedText.split(/\n\s*\n/)
  const rawChunks = []

  ...
}
```

它的分块策略是递归式的：

```
先按段落切
  ↓
段落太长就按句子切
  ↓
句子太长就按固定长度切
  ↓
最后加 overlap
```

这个比简单固定长度切分更适合 RAG。

因为 RAG 检索依赖语义完整性，如果直接每 500 字切一次，可能会把一个完整语义切断。

==`chunkBySentences`==

```js
function chunkBySentences(text, chunkSize) {
  const sentenceEnders = /(?<=[。！？.!?\n])\s*/g
  const sentences = text.split(sentenceEnders).filter(s => s.trim())

  ...
}
```

它负责把超长段落按句子切开。

支持中文和英文常见句末符号：

```
。 ！ ？ . ! ?
```

逻辑是：

```
遍历句子
  ↓
能放进当前 chunk 就追加
  ↓
放不下就新开一个 chunk
  ↓
如果单句仍然过长，再走固定长度切分
```

==`chunkByFixedSize`==

```js
function chunkByFixedSize(text, size, overlap) {
  const chunks = []
  const step = Math.max(size - overlap, 1)

  for (let i = 0; i < text.length; i += step) {
    chunks.push(text.slice(i, i + size))
  }

  return chunks
}
```

这是最终兜底方案。

它按固定长度切分，并支持 overlap。

比如：

```js
chunkSize = 500
chunkOverlap = 50
step = 450
```

那么切分窗口是：

```
0 - 500
450 - 950
900 - 1400
```

这样相邻 chunk 之间会保留一部分上下文，减少语义断裂。

##### 5. 这个 Worker 在上传链路中的作用

```
/api/upload/merge 返回 text
  ↓
useDocumentsAdvanced.chunkTextWithWorker(text)
  ↓
postMessage 给 textProcessor.worker.js
  ↓
recursiveChunk 分块
  ↓
postMessage 返回 chunks
  ↓
/api/embed 生成向量
```

面试里可以这样说：

> 上传文件合并并解析出文本后，我没有直接在主线程做大文本分块，而是把文本和分块参数传给 `public/workers/textProcessor.worker.js`。Worker 内部先按段落切分，如果段落超过 chunkSize，再按句子切分，如果句子仍然过长，最后按固定长度兜底，并根据 chunkOverlap 增加重叠内容。这样可以保证分块尽量语义完整，同时避免大文档处理阻塞 UI。

### 上传模块完整调用关系

最后把整个模块串起来：

```
1. 用户点击知识库按钮
   ↓
app/pages/index.vue
showDocPanel = true

2. 展示上传面板
   ↓
app/components/DocumentUpload.vue
接收 docState

3. 用户点击或拖拽文件
   ↓
handleFileSelect / handleDrop

4. 调用上传方法
   ↓
props.docState.uploadDocument(file)

5. 进入上传编排逻辑
   ↓
app/composables/useDocuments.js
useDocumentsAdvanced.uploadDocument(file)

6. 计算文件 hash
   ↓
computeFileHash(file)

7. 检查秒传 / 断点续传
   ↓
POST /api/upload/check

8. 上传缺失分片
   ↓
file.slice(start, end)
uploadChunkWithProgress()
POST /api/upload/chunk

9. 合并分片并解析文本
   ↓
POST /api/upload/merge

10. 文本分块
   ↓
chunkTextWithWorker()
public/workers/textProcessor.worker.js

11. 向量化入库
   ↓
POST /api/embed

12. 刷新文档列表
   ↓
loadDocuments()
GET /api/documents

13. UI 更新
   ↓
DocumentUpload.vue 展示：
✓ n 个块
```

### 文件上传原理

这个项目的文件上传不是普通的“一次性把文件丢给后端”，而是做成了一个比较完整的 **大文件上传 + RAG 入库流程**。

它的核心链路是：

```
选择文件
  ↓
计算文件 hash
  ↓
检查是否秒传 / 断点续传
  ↓
按 2MB 切分文件
  ↓
上传缺失分片
  ↓
服务端合并分片
  ↓
服务端解析文本
  ↓
前端 Web Worker 分块
  ↓
调用 /api/embed 向量化入库
  ↓
刷新知识库文档列表
```

当前实现中，前端核心逻辑在 `app/composables/useDocuments.js` 的 `useDocumentsAdvanced()`，上传面板在 `app/components/DocumentUpload.vue`，服务端接口主要是 `server/api/upload/check.post.js`、`server/api/upload/chunk.post.js`、`server/api/upload/merge.post.js`。

#### 1. 为什么要做分片上传？

普通上传是这样：

```
前端选择一个文件
  ↓
FormData 一次性上传整个文件
  ↓
服务端接收完整文件
```

这种方式简单，但是有几个问题：

```
文件稍大时，上传失败就要全部重传
网络中断后不能恢复
无法展示比较精确的上传进度
重复文件也会重复上传
```

所以这个项目采用的是 **文件分片上传**。

也就是把一个完整文件切成多个小块：

```
file.pdf
  ├─ chunk 0
  ├─ chunk 1
  ├─ chunk 2
  ├─ chunk 3
  └─ chunk n
```

当前项目里上传分片大小是：

```js
const chunkSize = 2 * 1024 * 1024
```

也就是每个分片 2MB。

如果一个文件是 9MB：

```js
totalChunks = Math.ceil(file.size / chunkSize)// Math.ceil(9 / 2) = 5
```

最终会切成 5 个分片。

#### 2. 文件 hash 的作用是什么？

上传前，项目会先计算文件的 SHA-256：

```js
const fileHash = await computeFileHash(file)
```

它的作用是给文件生成一个“内容指纹”。

```
同一个文件内容相同
  ↓
fileHash 相同

文件内容发生变化
  ↓
fileHash 不同
```

所以 `fileHash` 可以用来做两件事：

```
1. 秒传
2. 断点续传
```

#### 3. 秒传的原理

秒传并不是真的“瞬间上传成功”，而是：

```
前端先计算 fileHash
  ↓
请求 /api/upload/check
  ↓
服务端根据 fileHash 查找是否已有完整文档
  ↓
如果已有，直接返回 exists: true
  ↓
前端不再上传文件，直接提示秒传完成
```

当前项目服务端在 `server/api/upload/check.post.js` 里会调用 `findDocumentByHash(body.fileHash)` 判断是否已经存在相同文件。如果存在，就返回 `exists: true` 和对应文档信息。

可以理解成：

```
用户 A 上传过 a.pdf
  ↓
系统已经保存过它的 fileHash
  ↓
用户再次上传同一个 a.pdf
  ↓
服务端发现 hash 已存在
  ↓
直接复用已有文档
```

面试可以这样说：

> 秒传的核心不是提升网络速度，而是通过文件 hash 判断服务端是否已经存在相同内容的文件。如果存在，就直接复用已有文件和向量数据，不再重复上传。

#### 4. 断点续传的原理

断点续传解决的是：

```
文件上传到一半失败了
下次不想从头传
只想传剩下的部分
```

当前项目的流程是：

```
前端计算 fileHash
  ↓
请求 /api/upload/check
  ↓
服务端查 uploads/chunks/{fileHash} 目录
  ↓
返回已经上传过的 chunkIndex
  ↓
前端跳过这些分片
  ↓
只上传缺失分片
```

比如一个文件有 5 个分片：

```
0 1 2 3 4
```

第一次上传时，已经成功上传了：

```
0 1 2
```

网络断了。

第二次重新上传时，服务端返回：

```
uploadedChunks = [0, 1, 2]
```

前端循环上传时会判断：

```js
if (uploadedChunkSet.has(index)) continue
```

所以只会继续上传：

```
3 4
```

这就是断点续传。

服务端的 `server/utils/uploadSessions.js` 会维护上传会话、分片目录、已上传分片索引和已上传字节数，`check.post.js` 会把这些信息返回给前端。

#### 5. 分片上传是怎么做的？

前端用 `file.slice(start, end)` 切片。

核心逻辑可以简化理解成：

```js
for (let index = 0; index < totalChunks; index++) {
  if (uploadedChunkSet.has(index)) continue

  const start = index * chunkSize
  const end = Math.min(start + chunkSize, file.size)
  const chunk = file.slice(start, end)

  await uploadChunkWithProgress({
    chunk,
    chunkIndex: index,
    totalChunks,
    fileHash
  })
}
```

每个分片上传时，会携带这些元信息：

```
fileHashfileName
fileSize
chunkIndex
totalChunks
chunkSize
ext
```

服务端 `server/api/upload/chunk.post.js` 接收到分片后，会把它保存成：

```
uploads/chunks/{fileHash}/{chunkIndex}.part
```

比如：

```
uploads/chunks/abc123/0.part
uploads/chunks/abc123/1.part
uploads/chunks/abc123/2.part
```

当前项目服务端会校验分片索引、文件元信息，然后把分片写入对应路径。

#### 6. 上传进度是怎么算的？

项目里不是只看当前分片的进度，而是算整个文件的总进度。

核心思路是：

```
整体已上传字节数 = 已确认上传的分片字节数 + 当前分片实时上传字节数
```

比如：

```
文件总大小：10MB
已经完成分片：4MB
当前分片已上传：1MB
```

那么当前总进度就是：

```js
currentBytes = 4MB + 1MB
percent = currentBytes / 10MB * 100
```

项目里用 `updateProgressState()` 统一维护：

```
uploadProgress
uploadedBytes
totalBytes
uploadPercent
```

所以页面组件只负责展示，不关心进度计算细节。

#### 7. 分片合并的原理

当前端所有分片都上传完后，会请求：

```
POST /api/upload/merge
```

服务端做几件事：

```
1. 读取上传会话
2. 检查分片是否齐全
3. 按 chunkIndex 从 0 到 totalChunks - 1 顺序读取分片
4. 使用 writeStream 合并成完整文件
5. 调用 parseDocument 解析文本
6. 写入文档 meta 信息
7. 删除临时分片目录
8. 删除上传会话文件
9. 返回解析后的文本给前端
```

关键点是：**分片必须按顺序合并**。

```
0.part + 1.part + 2.part + ... + n.part  ↓完整文件
```

当前项目的 `merge.post.js` 会检查缺失分片，如果还有分片没上传完，就直接报错，不允许合并。

#### 8. 为什么文件上传后还要分块？

这里要区分两个 chunk：

```
文件分片 chunk
  ↓
用于上传、断点续传、秒传

文本分块 chunk
  ↓
用于 RAG 检索、embedding、向量化
```

文件上传完成后，服务端会解析出文本。

然后前端把文本交给 Web Worker 做语义分块：

```
完整文本
  ↓
按段落切
  ↓
段落太长按句子切
  ↓
句子太长按固定长度切
  ↓
加 overlap
  ↓
得到文本 chunks
```

最后这些文本 chunks 会发给：

```
POST /api/embed
```

服务端生成 embedding，并写入 ChromaDB。`server/api/embed.post.js` 会批量调用 embedding，每批最多 25 条，然后把 ids、embeddings、metadatas、documents 存入 Chroma Collection。

### 优化
#### 文件 hash 计算可以优化

当前项目使用：

```js
const buffer = await file.arrayBuffer()
const digest = await crypto.subtle.digest('SHA-256', buffer)
```

这种方式简单，但是它会一次性把整个文件读进内存。

当前项目限制最大 20MB，所以问题不大。服务端也在 `uploadSessions.js` 里限制了最大文件大小是 20MB。

但是如果以后支持更大的文件，比如 200MB、1GB，这种方式就不太合适。

**可以优化成增量 hash**

思路是：

```
按分片读取文件
  ↓
每读一片就更新 hash
  ↓
不用一次性把整个文件放进内存
```

可以把 hash 计算也放到 Web Worker 里：

```
主线程选择文件
  ↓
把文件传给 hash worker
  ↓
worker 分片读取并计算 hash
  ↓
计算完成后返回 fileHash
```

面试可以这样说：

> 当前项目因为限制 20MB，所以直接用 `file.arrayBuffer()` 计算 SHA-256 是可以接受的。但如果后续支持更大文件，我会把 hash 计算改成分片增量计算，并放到 Web Worker 中，避免大文件读取和哈希计算阻塞主线程。

#### 分片上传可以增加并发控制

当前上传流程是：

```
上传 chunk 0
  ↓
上传 chunk 1
  ↓
上传 chunk 2
  ↓
上传 chunk 3
```

也就是串行上传。

串行上传的优点是简单、稳定、容易控制进度。

缺点是速度可能不够快。

**可以改成有限并发上传**

比如同时上传 3 个分片：

```
chunk 0   chunk 1   chunk 2
   ↓         ↓         ↓
chunk 3   chunk 4   chunk 5
```

可以用一个并发池：

```js
const concurrency = 3
const queue = chunks.filter(chunk => !uploadedChunkSet.has(chunk.index))

async function uploadWithPool(queue) {
  const workers = new Array(concurrency).fill(0).map(async () => {
    while (queue.length) {
      const chunk = queue.shift()
      await uploadChunk(chunk)
    }
  })

  await Promise.all(workers)
}
```

优化后可以提升上传速度。

但要注意：

```
并发数不能太高
否则浏览器请求过多
弱网环境下失败率可能升高
服务端压力也会变大
```

面试可以这样说：

> 当前项目是串行上传分片，逻辑稳定但速度一般。后续可以加有限并发池，比如同时上传 3 到 5 个分片，在保证服务端压力可控的情况下提升上传速度。

#### 分片上传可以增加失败重试

当前如果某个分片上传失败，整个上传流程会进入 catch。

可以优化成：

```
单个分片失败
  ↓
重试 2~3 次
  ↓
仍然失败才终止整个上传
```

示例逻辑：

```js
async function uploadChunkWithRetry(chunk, retry = 3) {
  for (let i = 0; i < retry; i++) {
    try {
      return await uploadChunk(chunk)
    } catch (err) {
      if (i === retry - 1) throw err
    }
  }
}
```

还可以加指数退避：

```
第一次失败：等待 500ms
第二次失败：等待 1000ms
第三次失败：等待 2000ms
```

这样在弱网场景下体验会更好。

#### 可以支持暂停、继续、取消上传

当前项目有断点续传，但没有明确的“暂停 / 继续 / 取消”按钮。

可以增加：

```
暂停上传
继续上传
取消上传
```

核心可以用：

```js
AbortController
```

或者当前项目里使用的是 `XMLHttpRequest`，也可以保存当前 xhr 实例：

```js
let currentXhr = null

function cancelUpload() {
  currentXhr?.abort()
}
```

状态设计可以是：

```
idle
hashing
checking
uploading
paused
merging
chunking
embedding
success
failed
canceled
```

面试可以这样说：

> 当前项目支持断点续传，但还没有显式的暂停和取消控制。后续可以维护上传任务状态，并保存当前请求控制器，用户点击暂停时 abort 当前分片请求，点击继续时重新请求 `/api/upload/check` 获取已上传分片，再继续上传缺失部分。

#### 服务端可以校验最终文件 hash

当前服务端支持可选的 `chunkHash` 校验，`chunk.post.js` 里有 `chunkHash` 判断逻辑，但前端当前上传分片时没有明显传这个字段。

可以优化成两层校验：

```
1. 每个分片上传时校验 chunkHash
2. 所有分片合并后校验 fullFileHash
```

完整流程：

```
前端计算 fileHash
  ↓
上传分片时带 chunkHash
  ↓
服务端校验每个分片是否损坏
  ↓
合并完成后重新计算完整文件 hash
  ↓
和前端 fileHash 比较
  ↓
一致才进入解析和入库
```

这样可以避免：

```
分片传输过程中损坏
分片顺序错误
服务端合并文件异常
```

#### 文本分块和向量化可以后移到服务端

当前项目的流程是：

```
服务端合并并解析文本
  ↓
把 text 返回给前端
  ↓
前端 Web Worker 分块
  ↓
前端把 chunks 再发给 /api/embed
```

这个设计的好处是：

```
前端可以控制 chunkSize / chunkOverlap
可以展示更细的处理阶段
Web Worker 避免主线程卡顿
```

但缺点是：

```
大文本要从服务端返回到前端
前端分块后又要把 chunks 发回服务端
数据来回传输一遍
```

如果文档变大，可以优化成：

```
服务端合并文件
  ↓
服务端解析文本
  ↓
服务端直接分块
  ↓
服务端直接 embedding
  ↓
前端只轮询处理状态
```

也就是把 RAG 入库变成后端任务。

面试可以这样说：

> 当前项目把文本分块放在前端 Worker 里，能体现前端性能优化能力。但从工程角度看，如果文档更大或多用户并发更多，我会考虑把解析、分块、embedding 全部放到服务端异步任务里，前端只负责上传和展示任务进度，这样可以减少文本在前后端之间的重复传输。

#### 可以增加上传任务队列

当前上传面板每次只处理一个文件：

```js
const file = e.target.files?.[0]
```

如果要支持多文件上传，可以改成：

```js
const files = Array.from(e.target.files || [])
```

然后维护任务队列：

```js
uploadTasks = [
  { file, status: 'waiting', progress: 0 },
  { file, status: 'uploading', progress: 30 },
  { file, status: 'success', progress: 100 }
]
```

队列模式可以支持：

```
多文件排队上传
单个文件失败不影响其他文件
每个文件单独展示进度
每个文件单独取消 / 重试
```

面试可以这样说：

> 当前项目偏单文件上传，适合个人知识库场景。后续如果支持批量导入文档，可以把上传状态从单个 `isUploading` 扩展成任务队列，每个任务维护自己的 hash、进度、状态和错误信息。

### 问答
#### 如何保证分片合并后的文件是正确的？

可以答：

> 首先服务端会检查分片数量是否完整，只有所有 chunkIndex 都存在时才允许合并。其次合并时必须按照 chunkIndex 从小到大顺序写入。
> 
> 更严谨的优化是：前端上传每个分片时带上 chunkHash，服务端校验每个分片；最终合并后服务端再计算完整文件 hash，和前端上传前计算的 fileHash 比较，一致才认为文件有效。

#### 如果某个分片上传失败怎么办？

可以答：

> 当前项目如果某个分片失败，会进入异常处理，提示上传失败。后续可以优化为分片级重试，比如每个分片最多重试 3 次，并配合指数退避。这样弱网情况下不会因为一次网络抖动就导致整个上传任务失败。
> 
> 如果重试后仍然失败，可以保留已经上传成功的分片。用户下次继续上传时，通过 `/api/upload/check` 拿到已上传分片列表，再继续传缺失部分。

#### 上传进度为什么不能简单用当前请求进度？

可以答：

> 因为当前项目是多个分片组成一个完整文件。单个分片的进度只能表示当前 chunk 上传了多少，不能代表整个文件进度。
> 
> 所以我会用“已完成分片的字节数 + 当前分片已上传字节数”来计算整体进度，再除以文件总大小得到百分比。这样用户看到的是整个文件级别的上传进度。


## 2. 文档解析组件
### 2.1 文件解析入口组件

> `server/api/upload/merge.post.js`

这个文件是当前分片上传流程里的 **解析入口**。

也就是说，在高级上传流程中，文件不是上传完立刻解析，而是：

```
前端上传所有分片
  ↓
服务端合并成完整文件
  ↓
读取完整文件 Buffer
  ↓
调用 parseDocument(fileBuffer, session.fileName)
  ↓
提取纯文本
```

在 `merge.post.js` 里，合并文件后会读取完整文件：

```js
const fileBuffer = await readFile(filePath)
const text = await parseDocument(fileBuffer, session.fileName)
```

然后判断是否提取到了有效文本：

```js
if (!text?.trim()) {
  throw new Error('未能从文件中提取到文本内容')
}
```

==作用==：`merge.post.js` 是 **分片上传流程中的文档解析触发点**。它负责在文件分片合并完成后，把完整文件交给解析工具 `parseDocument`，拿到纯文本后再生成文档元信息。源码里它还会写入 `docMeta`，包括 `id / name / ext / size / textLength / createdAt / fileHash / hasVectors / chunksCount` 等信息。

### 2.2 兼容旧版直传的解析入口

> `server/api/upload.post.js`

这个文件是另一个上传入口，属于 **普通直传上传接口**。

它没有走分片上传，而是直接：

```
接收 multipart 文件
  ↓
校验文件类型
  ↓
校验文件大小
  ↓
保存原始文件
  ↓
调用 parseDocument 提取文本
  ↓
写入文档元信息
  ↓
返回 text
```

里面同样会调用：

```js
const text = await parseDocument(file.data, file.filename)
```

==作用==：`upload.post.js` 是普通上传模式下的解析入口。它适合小文件或早期版本逻辑：前端一次性上传完整文件，服务端保存文件后立即解析文本。当前首页使用的是 `useDocumentsAdvanced()`，主要走分片上传链路，但这个文件仍然是项目里的一个解析入口。

### 2.3 文档解析核心模块

> `server/utils/documentParser.js`

这是文件解析组件最核心的模块。

它导出了一个函数：

```js
export async function parseDocument(buffer, filename)
```

它接收两个参数：

```
buffer：文件二进制内容
filename：文件名，用来判断文件类型
```

返回值是：

```
提取出来的纯文本 string
```

源码里根据文件后缀做分发：

```
pdf   → pdf-parse
docx  → mammoth
txt   → Buffer.toString('utf-8')
md    → Buffer.toString('utf-8')
其他  → 抛出不支持文件类型错误
```

==作用==：`documentParser.js` 是 **文件类型分发器 + 具体解析器封装层**。业务接口不需要关心 PDF 怎么解析、DOCX 怎么解析，只需要把 `buffer` 和 `filename` 传给 `parseDocument`。具体解析逻辑都集中在这个工具模块里。

### 2.4 PDF 解析子模块

> `server/utils/documentParser.js` 中的 `case 'pdf'`

项目里 PDF 解析使用的是：

```js
import pdf from 'pdf-parse'
```

对应逻辑是：

```js
case 'pdf': {
  const data = await pdf(Buffer.from(buffer))
  return data.text || ''
}
```

它做的事情是：

```
接收 PDF Buffer
  ↓
调用 pdf-parse 解析 PDF
  ↓
从 data.text 中拿到文本
  ↓
返回纯文本
```

==作用==：PDF 解析子模块负责把 PDF 文件里的文字内容提取出来，供后续文本分块和向量化使用。它的输出不是结构化页面对象，而是纯文本字符串。

需要注意：这个方案主要适合 **文本型 PDF**。如果是扫描件 PDF，里面本质上是图片，没有真实文字层，`pdf-parse` 通常提取不到有效文本。

### 2.5 DOCX 解析子模块

> `server/utils/documentParser.js` 中的 `case 'docx'`

项目里 DOCX 解析使用的是：

```js
import mammoth from 'mammoth'
```

对应逻辑是：

```js
case 'docx': {
  const result = await mammoth.extractRawText({ buffer: Buffer.from(buffer) })
  return result.value || ''
}
```

它做的事情是：

```
接收 DOCX Buffer
  ↓
调用 mammoth.extractRawText
  ↓
提取 Word 文档里的原始文本
  ↓
返回 result.value
```

==作用==：DOCX 解析子模块负责把 Word 文档里的段落文本提取出来。这里用的是 `extractRawText`，所以它更偏向“拿纯文本”，而不是保留复杂样式、图片、表格结构。

### 2.6 TXT / Markdown 解析子模块

> `server/utils/documentParser.js` 中的 `case 'txt'` 和 `case 'md'`

对应逻辑是：

```js
case 'txt':
case 'md': {
  return Buffer.from(buffer).toString('utf-8')
}
```

它做的事情很简单：

```
接收文件 Buffer
  ↓
按 utf-8 转成字符串
  ↓
直接返回文本
```

==作用==：TXT 和 Markdown 本身就是文本格式，不需要额外解析库，所以直接把 Buffer 转成 UTF-8 字符串即可。

这里要注意一个点：

```
Markdown 当前只是作为普通文本读取
并没有解析 Markdown AST
也没有单独保留标题层级、代码块、引用等结构
```

所以后续如果优化 RAG 效果，Markdown 是一个很好的优化点。

### 2.7 文件类型校验组件

文件类型校验目前出现在两个位置。

#### 2.7.1 普通直传接口中的校验

> `server/api/upload.post.js`

里面定义：

```js
const allowedExts = ['pdf', 'txt', 'md', 'docx']
```

然后判断：

```js
if (!allowedExts.includes(ext)) {
  throw createError({
    statusCode: 400,
    message: `不支持的文件类型: .${ext}`
  })
}
```

==作用==：这个校验用于普通直传模式，防止不支持的文件进入解析流程。

#### 2.7.2 分片上传流程中的校验

> `server/utils/uploadSessions.js`

分片上传流程中，文件类型校验在上传会话工具里：

```js
const ALLOWED_EXTS = ['pdf', 'txt', 'md', 'docx']
```

以及：

```js
validateUploadMeta(...)
```

==作用==：这个校验用于分片上传流程。因为分片上传分为 `check / chunk / merge` 多个接口，所以文件类型校验被抽到了 `uploadSessions.js` 里复用。服务端会限制只允许 `pdf / txt / md / docx`，并限制文件大小不能超过 20MB。

### 2.8 文档元信息生成组件

文件解析完成后，项目会生成文档元信息。

在分片上传链路里，对应位置是：

> `server/api/upload/merge.post.js`

解析完成后生成：

```js
const docMeta = {
  id: session.documentId,
  name: session.fileName,
  ext,
  size: session.fileSize,
  textLength: text.length,
  createdAt: new Date().toISOString(),
  fileHash: body.fileHash,
  hasVectors: false,
  chunksCount: 0
}
```

==作用==：这个模块负责把“一个原始文件”转成系统里的“知识库文档记录”。其中 `textLength` 表示解析出的文本长度，`hasVectors` 初始为 `false`，表示还没有完成向量化；`chunksCount` 初始为 `0`，后面 `/api/embed` 向量化成功后会更新。

### 当前解析模块组成

```
文件解析组件
  ├─ 解析入口：server/api/upload/merge.post.js
  ├─ 旧版直传入口：server/api/upload.post.js
  ├─ 核心解析器：server/utils/documentParser.js
  │   ├─ PDF 解析：pdf-parse
  │   ├─ DOCX 解析：mammoth.extractRawText
  │   └─ TXT / MD 解析：Buffer.toString('utf-8')
  ├─ 文件类型校验：upload.post.js / uploadSessions.js
  └─ 文档元信息生成：merge.post.js
```

### 面试里可以这样说

> 文件解析组件主要在服务端实现，核心模块是 `server/utils/documentParser.js`。它暴露了一个 `parseDocument(buffer, filename)` 方法，根据文件后缀做解析分发：PDF 使用 `pdf-parse` 提取文本，DOCX 使用 `mammoth.extractRawText` 提取 Word 文档中的原始文本，TXT 和 MD 直接通过 `Buffer.toString('utf-8')` 转成字符串。
> 
> 在当前分片上传流程中，解析入口在 `server/api/upload/merge.post.js`。服务端先把所有分片按顺序合并成完整文件，然后读取文件 Buffer，调用 `parseDocument(fileBuffer, session.fileName)` 提取文本。如果没有提取到有效文本，就直接返回错误；如果成功，就生成文档元信息，包括文件名、类型、大小、文本长度、fileHash、向量化状态和 chunk 数量。
> 
> 项目里还保留了一个普通直传入口 `server/api/upload.post.js`，它也会在接收完整文件后调用 `parseDocument`，但当前高级上传流程主要走的是 `check / chunk / merge` 这套分片上传链路。


### 模块完整流程

先明确一点：**文件解析模块没有独立的前端页面组件**。

它不是一个单独的 Vue 页面，而是嵌在“文档上传链路”里的一个阶段。

当前项目的解析流程是：

```
DocumentUpload.vue
  ↓ 用户选择 / 拖拽文件
useDocumentsAdvanced.uploadDocument(file)
  ↓ 上传分片
/api/upload/merge
  ↓ 合并文件后调用 parseDocument
server/utils/documentParser.js
  ↓ 根据文件类型提取文本
返回 text
  ↓ 前端拿到 text 后交给 Web Worker 分块
/api/embed
  ↓ 向量化入库
```

所以讲文件解析模块时，应该分成四层：

```
1. 前端上传面板：DocumentUpload.vue
2. 前端流程编排：useDocumentsAdvanced()
3. 服务端解析入口：upload/merge.post.js
4. 核心解析工具：documentParser.js
```

#### 解析状态展示入口

>文件：`app/components/DocumentUpload.vue`

它不是解析逻辑本身，但它是用户能看到“文件正在解析”的地方。

上传组件里真正和解析相关的是：

```
上传进度展示区域
错误提示区域
文档列表状态区域
```

源码里可以看到，组件通过 `docState.uploadProgress.value` 展示当前上传 / 解析 / 分块 / 向量化阶段，通过 `docState.error.value` 展示错误，通过 `docState.documents.value` 展示文档是否已向量化。

##### 1. template 结构

文件解析相关的 template 可以抽成三块。

###### 1.1 上传进度区域

```vue
<div v-if="docState.isUploading.value" class="upload-progress">
  <span class="spinner"></span>

  <p>{{ docState.uploadProgress.value }}</p>

  <div class="progress-track">
    <div
      class="progress-fill"
      :style="{ width: `${docState.uploadPercent.value || 0}%` }"
    ></div>
  </div>

  <p class="upload-stats">
    {{ formatSize(docState.uploadedBytes.value) }}
    /
    {{ formatSize(docState.totalBytes.value) }}
    ({{ docState.uploadPercent.value || 0 }}%)
  </p>
</div>
```

它展示的是整个文件处理流程的状态，不只是上传进度。

比如上传链路中会出现这些状态：

```
正在上传分片 1/5...
正在合并文件并提取文本...
正在分块处理...
正在生成向量...
上传完成
```

其中：

```
正在合并文件并提取文本...
```

就是文件解析阶段的前端提示。

###### 1.2 错误提示区域

```vue
<div v-if="docState.error.value" class="alert alert-error">
  {{ docState.error.value }}
</div>
```

如果解析失败，比如：

```
文件中未提取到有效文本
未能从文件中提取到文本内容
不支持的文件类型
```

这些错误会通过 `docState.error.value` 展示到页面上。

###### 1.3 文档列表区域

```vue
<div
  v-for="doc in docState.documents.value"
  :key="doc.id"
  class="doc-item"
>
  <div class="doc-icon" :class="`doc-${doc.ext}`">
    {{ doc.ext.toUpperCase() }}
  </div>

  <div class="doc-info">
    <div class="doc-name">{{ doc.name }}</div>

    <div class="doc-meta">
      <span>{{ formatSize(doc.size) }}</span>

      <span v-if="doc.hasVectors" class="doc-status done">
        ✓ {{ doc.chunksCount }} 个块
      </span>

      <span v-else class="doc-status pending">
        待向量化
      </span>
    </div>
  </div>
</div>
```

这里能看到文件解析后的结果状态。

解析成功后，服务端会生成文档元信息。

向量化成功后，文档会显示：

```
✓ n 个块
```

如果还没向量化，则显示：

```
待向量化
```

##### 2. 核心变量

==`docState.uploadProgress`==

```js
docState.uploadProgress.value
```

它是前端展示解析阶段的核心变量。

在解析阶段，`useDocumentsAdvanced()` 会设置：

```
正在合并文件并提取文本...
```

这个文案会直接显示在上传面板中。

==`docState.error`==

```js
docState.error.value
```

用于展示解析错误。

如果服务端 `/api/upload/merge` 返回错误，前端 catch 后会把错误信息赋给 `error`。

例如：

```
未能从文件中提取到文本内容
文件中未提取到有效文本
合并文件失败
```

==`docState.documents`==

```js
docState.documents.value
```

解析成功后，服务端会写入文档 meta。

前端调用 `loadDocuments()` 后，会刷新这个列表。

文档中和解析相关的字段包括：

```
name        文件名
ext         文件类型
size        文件大小
textLength  解析出的文本长度
hasVectors  是否已经向量化
chunksCount 文本块数量
```

##### 3. 核心函数

==`handleFileSelect`==

```js
async function handleFileSelect(e) {
  const file = e.target.files?.[0]
  if (file) {
    await props.docState.uploadDocument(file)
    e.target.value = ''
  }
}
```

点击选择文件后，最终会调用：

```js
props.docState.uploadDocument(file)
```

也就是说，解析流程不是在 `DocumentUpload.vue` 里直接发生的，而是由它触发上传编排函数。

==`handleDrop`==

```js
async function handleDrop(e) {
  isDragging.value = false
  const file = e.dataTransfer?.files?.[0]
  if (file) {
    await props.docState.uploadDocument(file)
  }
}
```

拖拽上传也是一样。

不管是点击选择还是拖拽上传，最终都进入同一个入口：

```js
uploadDocument(file)
```

##### 4. 在链路中的作用

`DocumentUpload.vue` 在文件解析链路中的作用是：

```
1. 接收用户文件
2. 触发 uploadDocument(file)
3. 展示“合并文件并提取文本”的阶段状态
4. 展示解析失败错误
5. 展示解析后生成的文档记录
```

面试里可以这样说：

> `DocumentUpload.vue` 不负责真正解析文件，它是解析流程的 UI 入口。用户选择或拖拽文件后，组件调用 `docState.uploadDocument(file)`。解析过程中，组件通过 `uploadProgress` 展示“正在合并文件并提取文本”，如果解析失败，通过 `error` 展示错误信息；解析和向量化成功后，通过文档列表展示文件类型、大小和文本块数量。

#### 解析流程调度层

>文件：`app/composables/useDocuments.js`

当前首页使用的是：

```js
const docState = useDocumentsAdvanced()
```

这个 composable 是上传、解析、分块、向量化的前端编排层。

它本身不解析 PDF / DOCX，但它负责在合适的时机调用服务端解析接口，并处理服务端返回的文本。

##### 1. template 结构

这个文件不是 Vue 页面组件，没有 template。

它是 composable 逻辑模块，主要通过返回响应式变量和函数给 `DocumentUpload.vue` 使用。

可以理解成：

```
DocumentUpload.vue 负责展示
useDocumentsAdvanced() 负责业务流程
```

##### 2. 核心变量

==`uploadProgress`==

```js
const uploadProgress = ref('')
```

解析阶段会被设置成：

```js
uploadProgress.value = '正在合并文件并提取文本...'
```

它把解析状态传给上传组件展示。

==`error`==

```js
const error = ref(null)
```

解析失败时会写入：

```js
error.value = err.data?.message || err.message || '上传失败'
```

比如服务端返回：

```
未能从文件中提取到文本内容
```

前端就会把它显示在上传面板上。

==`result`==

```js
const result = await $fetch('/api/upload/merge', {
  method: 'POST',
  body: {
    fileName: file.name,
    fileSize: file.size,
    fileHash,
    ext,
    chunkSize,
    totalChunks
  }
})
```

`result` 是服务端合并和解析后的返回值。

它里面最关键的是：

```
result.id     文档 id
result.text   解析出来的纯文本
```

后续文本分块依赖的就是 `result.text`。

==`chunks`==

```js
const chunks = await chunkTextWithWorker(result.text, {
  chunkSize: settings.chunkSize || 500,
  chunkOverlap: settings.chunkOverlap || 50
})
```

解析模块的输出是 `text`，而 RAG 入库需要的是 `chunks`。

所以 `useDocumentsAdvanced()` 会把解析结果继续交给 Web Worker 分块。

##### 3. 核心函数

==`uploadDocument`==

这是文件解析流程在前端的主入口。

和解析相关的部分是：

```js
uploadProgress.value = '正在合并文件并提取文本...'

const result = await $fetch('/api/upload/merge', {
  method: 'POST',
  body: {
    fileName: file.name,
    fileSize: file.size,
    fileHash,
    ext,
    chunkSize,
    totalChunks
  }
})

if (!result.text || !result.text.trim()) {
  throw new Error('文件中未提取到有效文本')
}
```

这段逻辑做了三件事：

```
1. 通知 UI 当前进入“合并 + 解析”阶段
2. 调用 /api/upload/merge
3. 校验服务端是否返回了有效文本
```

注意：

```
文件解析实际发生在服务端
前端只负责触发 merge 接口并接收 text
```

==`chunkTextWithWorker`==

```js
const chunks = await chunkTextWithWorker(result.text, {
  chunkSize: settings.chunkSize || 500,
  chunkOverlap: settings.chunkOverlap || 50
})
```

它不是解析函数，但它是解析后的下一步。

完整关系是：

```
parseDocument 提取 text
  ↓
chunkTextWithWorker 把 text 切成 chunks
```

所以面试中要强调：

```
文件解析负责把文件转成 text
文本分块负责把 text 转成 chunks
```

这两个阶段不要混淆。

##### 4. 在链路中的作用

`useDocumentsAdvanced()` 在解析链路中是 **流程调度层**。

它负责：

```
1. 在分片上传完成后调用 /api/upload/merge
2. 把 UI 状态切换为“正在合并文件并提取文本”
3. 接收服务端返回的 text
4. 校验 text 是否为空
5. 将 text 交给 Web Worker 分块
6. 将 chunks 交给 /api/embed 向量化
```

面试里可以这样说：

> 文件解析实际在服务端完成，前端的 `useDocumentsAdvanced()` 主要负责流程编排。所有分片上传完成后，它会把状态改成“正在合并文件并提取文本”，然后调用 `/api/upload/merge`。服务端返回解析出的 `text` 后，前端会先校验文本是否为空，再把文本传给 Web Worker 做分块，最后调用 `/api/embed` 做向量化入库。

#### 分片合并 + 文件解析入口

>文件：`server/api/upload/merge.post.js`

这是当前高级上传流程里真正触发解析的服务端接口。

它的职责不只是合并分片，还包括：

```
校验上传会话
检查分片完整性
合并完整文件
调用 parseDocument 提取文本
生成文档元信息
清理临时分片
返回文本给前端
```

源码里可以看到，文件合并后会读取完整文件 Buffer，并调用 `parseDocument(fileBuffer, session.fileName)` 提取文本。

##### 1. template 结构

这是 Nuxt server API 文件，没有 template。

它是服务端接口模块。

可以把它理解成：

```
前端请求 /api/upload/merge
  ↓
merge.post.js 执行合并和解析
  ↓
返回 docMeta + text
```

##### 2. 核心变量

==`body`==

```js
const body = await readBody(event)
```

前端传来的上传元信息。

里面包括：

```js
fileName
fileSize
fileHash
ext
chunkSize
totalChunks
```

这些信息用于：

```
校验文件类型
读取上传会话
确定分片数量
定位分片目录
生成文档元信息
```

==`ext`==

```js
const ext = validateUploadMeta(body)
```

校验并返回文件后缀。

支持的类型来自上传会话工具：

```
pdf
txt
md
docx
```

如果文件类型不支持，会直接抛错，不进入解析阶段。

==`session`==

```js
const session = await readUploadSession(body.fileHash)
```

上传会话里记录了这次上传的核心信息：

```
documentId
fileHash
fileName
fileSize
ext
chunkSize
totalChunks
```

解析阶段需要 `session.fileName` 判断文件类型，也需要 `session.documentId` 生成文档记录。

==`uploadedChunks`==

```js
const uploadedChunks = await listUploadedChunkIndexes(body.fileHash)
```

表示服务端当前已经收到哪些分片。

比如：

```
[0, 1, 2, 3]
```

==`missingChunks`==

```
const missingChunks = []
```

服务端会遍历：

```
0 到 totalChunks - 1
```

检查是否有分片缺失。

如果缺失，就不能合并，更不能解析。

这一步可以避免解析一个不完整文件。

==`filePath`==

```js
const filePath = getDataPath('documents', `${session.documentId}.${ext}`)
```

完整文件最终保存的位置。

比如：

```
data/documents/{documentId}.pdf
```

==`fileBuffer`==

```js
const fileBuffer = await readFile(filePath)
```

合并后的完整文件 Buffer。

这个 Buffer 会传给：

```js
parseDocument(fileBuffer, session.fileName)
```

==`text`==

```js
const text = await parseDocument(fileBuffer, session.fileName)
```

这是文件解析模块最重要的产物。

它是从 PDF / DOCX / TXT / MD 中提取出来的纯文本。

后续：

```
text
  ↓
Web Worker 分块
  ↓
embedding
  ↓
向量库
```

==`docMeta`==

```js
const docMeta = {
  id: session.documentId,
  name: session.fileName,
  ext,
  size: session.fileSize,
  textLength: text.length,
  createdAt: new Date().toISOString(),
  fileHash: body.fileHash,
  hasVectors: false,
  chunksCount: 0
}
```

这是解析成功后生成的文档元信息。

其中和解析强相关的是：

```
textLength
```

它记录解析出的文本长度。

`hasVectors` 和 `chunksCount` 初始值是：

```
hasVectors: falsechunksCount: 0
```

因为此时只是解析完成，还没有向量化。

##### 3. 核心函数 / 调用

==`validateUploadMeta`==

```js
const ext = validateUploadMeta(body)
```

作用是：

```
校验文件名
校验文件大小
校验文件 hash
校验分片
数量校验
文件类型
```

只有校验通过，才会继续合并和解析。

==`readUploadSession`==

```js
const session = await readUploadSession(body.fileHash)
```

作用是读取当前文件的上传会话。

如果没有 session，说明前面的 check / chunk 阶段可能没有正常完成。

此时会报错：

```
未找到上传会话，请重新开始上传
```

==`listUploadedChunkIndexes`==

```js
const uploadedChunks = await listUploadedChunkIndexes(body.fileHash)
```

作用是读取已经落盘的 `.part` 文件。

它配合 `missingChunks` 检查所有分片是否完整。

###### 分片完整性检查

```
for i from 0 to totalChunks - 1
  ↓
如果 uploadedChunks 不包含 i
  ↓
加入 missingChunks
```

如果有缺失分片：

```
仍有 n 个分片缺失，无法合并
```

这一步是解析前的保护。

因为如果文件不完整，PDF / DOCX 解析很容易失败，或者提取出错误文本。

###### 合并完整文件

```
创建写入流
  ↓
按 chunkIndex 顺序读取分片
  ↓
writer.write(chunkBuffer)
  ↓
writer.end()
```

重点是：

```
必须按分片索引顺序合并
```

否则文件内容顺序错乱，解析结果也会错误。

==`parseDocument`==

```JS
const text = await parseDocument(fileBuffer, session.fileName)
```

这是解析动作本身。

`merge.post.js` 不关心 PDF / DOCX 具体怎么解析，只负责把完整文件 Buffer 和文件名交给 `parseDocument`。

这就是一个典型的职责拆分：

```
merge.post.js
  ↓ 负责合并和流程控制

documentParser.js
  ↓ 负责不同格式文件的文本提取
```

###### 文本有效性校验

```JS
if (!text?.trim()) {
  throw new Error('未能从文件中提取到文本内容')
}
```

这一步非常重要。

因为有些文件虽然上传成功，但可能无法解析出文本，比如：

```
扫描版 PDF
空文件
乱码文件
损坏文件
只有图片的 DOCX
```

如果没有这一步，后面分块和向量化会处理空文本，浪费资源。

###### 写入文档元信息

```JS
await writeJSON(
  getDataPath('documents', `${session.documentId}.meta.json`),
  docMeta
)
```

解析成功后，写入文档元数据。

这一步意味着：

```
文件已经从“临时上传态”
  ↓
变成“知识库文档记录”
```
###### 清理临时文件

```
删除 uploads/chunks/{fileHash}
删除 uploads/{fileHash}.json
```

解析成功并生成文档记录后，临时分片和上传会话就不需要了。

##### 4. 在链路中的作用

`merge.post.js` 是文件解析链路里的 **服务端入口层**。

它负责：

```
1. 确认上传会话存在2. 确认所有分片完整3. 合并完整文件4. 调用 parseDocument 提取文本5. 校验文本有效性6. 生成文档元信息7. 清理上传临时文件8. 把 text 返回给前端
```

面试里可以这样说：

> `/api/upload/merge` 是文件解析真正开始的地方。前端上传完所有分片后，会请求这个接口。服务端先读取上传会话，检查分片是否完整，然后按 chunkIndex 顺序合并成完整文件。合并完成后读取文件 Buffer，调用 `parseDocument(fileBuffer, session.fileName)` 根据文件类型提取文本。如果文本为空，就直接报错；如果成功，就生成文档 meta，并把解析出的 text 返回给前端，供后续 Web Worker 分块使用。

---

#### 文件解析核心模块

>文件：`server/utils/documentParser.js`

这是整个文件解析模块的核心。

它对外只暴露一个函数：

```JS
parseDocument(buffer, filename)
```

作用是：

```
根据文件后缀
选择不同解析方式
返回纯文本
```

源码中它支持：

```
pdfdocxtxtmd
```

PDF 使用 `pdf-parse`，DOCX 使用 `mammoth.extractRawText`，TXT 和 MD 直接按 UTF-8 转字符串。

##### 1. template 结构

这是服务端工具函数文件，没有 template。

它是纯逻辑模块。

##### 2. 核心变量

==`buffer`==

```JS
parseDocument(buffer, filename)
```

`buffer` 是文件的二进制内容。

来源有两种：

```
分片上传流程：
  merge.post.js 合并完整文件后 readFile(filePath)

普通直传流程：
  upload.post.js 直接拿 multipart 文件 data
```

==`filename`==

```JS
parseDocument(buffer, filename)
```

`filename` 用来判断文件类型。

源码里通过：

```JS
const ext = filename.split('.').pop().toLowerCase()
```

拿到扩展名。

==`ext`==

```JS
const ext = filename.split('.').pop().toLowerCase()
```

`ext` 决定走哪个解析分支：

```
pdf   → pdf-parse
docx  → mammoth
txt   → Buffer.toString
md    → Buffer.toString
```

##### 3. 核心函数

==`parseDocument`==

```JS
export async function parseDocument(buffer, filename) {
  const ext = filename.split('.').pop().toLowerCase()

  switch (ext) {
    case 'pdf':
      ...
    case 'docx':
      ...
    case 'txt':
    case 'md':
      ...
    default:
      throw new Error(...)
  }
}
```

这是一个典型的策略分发函数。

它不把所有解析逻辑散落在业务接口里，而是集中封装在 `documentParser.js` 中。

好处是：

```
接口层只关心“我要提取文本”
解析层负责“不同格式怎么提取”
后续扩展新格式时，只改 documentParser.js
```

###### PDF 解析分支

```JS
case 'pdf': {
  const data = await pdf(Buffer.from(buffer))
  return data.text || ''
}
```

它的逻辑是：

```
PDF Buffer
  ↓
pdf-parse
  ↓
data.text
  ↓
返回纯文本
```

这里的重点是：

```
pdf-parse 主要适合文本型 PDF
扫描件 PDF 可能提取不到文本
```

面试中可以主动补一句：

> 当前 PDF 解析依赖 PDF 本身存在文字层，如果是扫描件 PDF，后续可以接 OCR。

###### DOCX 解析分支

```JS
case 'docx': {
  const result = await mammoth.extractRawText({
    buffer: Buffer.from(buffer)
  })
  return result.value || ''
}
```

它的逻辑是：

```
DOCX Buffer
  ↓
mammoth.extractRawText
  ↓
result.value
  ↓
返回纯文本
```

这里用的是 `extractRawText`，说明当前目标是：

```
提取纯文本
不保留复杂样式
不单独处理图片
不精细还原表格结构
```

###### TXT / MD 解析分支

```JS
case 'txt':
case 'md': {
  return Buffer.from(buffer).toString('utf-8')
}
```

TXT 和 Markdown 本身就是文本文件，所以直接转字符串。

但这里有一个可优化点：

```
当前 MD 只是当普通文本读取
没有解析标题、代码块、引用、列表结构
```

如果后续优化 RAG 效果，可以对 Markdown 做结构化解析。

###### 默认错误分支

```JS
default:
  throw new Error(`不支持的文件类型: .${ext}，仅支持 PDF、DOCX、TXT、MD`)
```

如果遇到不支持的类型，直接抛错。

比如：

```
xlsx
pptx
png
jpg
zip
```

都不会进入解析。

##### 4. 在链路中的作用

`documentParser.js` 是 **解析策略层**。

它负责：

```
1. 根据 filename 判断文件类型
2. 调用对应解析库
3. 把不同格式统一转成纯文本
4. 把文本返回给上层接口
```

面试里可以这样说：

> `documentParser.js` 是文件解析的核心模块。它对外暴露 `parseDocument(buffer, filename)`，上层接口不需要关心具体文件类型怎么处理，只要传入文件 Buffer 和文件名即可。内部根据扩展名分发：PDF 用 `pdf-parse` 提取 `data.text`，DOCX 用 `mammoth.extractRawText` 提取 `result.value`，TXT 和 MD 直接按 UTF-8 转字符串。最终统一返回纯文本，供后续文本分块和向量化使用。

#### 完整调用关系

文件解析模块可以这样串起来：

```
1. 用户上传文件
   ↓
DocumentUpload.vue
handleFileSelect / handleDrop

2. 前端进入上传编排
   ↓
useDocumentsAdvanced.uploadDocument(file)

3. 分片上传完成
   ↓
uploadProgress = "正在合并文件并提取文本..."

4. 请求合并接口
   ↓
POST /api/upload/merge

5. 服务端读取上传会话
   ↓
readUploadSession(fileHash)

6. 服务端检查分片完整性
   ↓
listUploadedChunkIndexes(fileHash)

7. 服务端合并完整文件
   ↓
0.part + 1.part + ... + n.part

8. 服务端读取完整文件 Buffer
   ↓
readFile(filePath)

9. 调用核心解析器
   ↓
parseDocument(fileBuffer, session.fileName)

10. 根据文件类型分发
   ↓
pdf   → pdf-parse
docx  → mammoth.extractRawText
txt   → Buffer.toString('utf-8')
md    → Buffer.toString('utf-8')

11. 返回纯文本 text
   ↓
merge.post.js 校验 text 是否为空

12. 写入文档元信息
   ↓
documents/{documentId}.meta.json

13. 返回 text 给前端
   ↓
useDocumentsAdvanced 接收 result.text

14. 前端继续处理
   ↓
Web Worker 分块
   ↓
/api/embed 向量化
```

### 文件解析原理

当前项目的文件解析核心在：

> `server/utils/documentParser.js`

它对外暴露：

```js
parseDocument(buffer, filename)
```

然后根据文件后缀分发不同解析策略：

```
pdf   → pdf-parse
docx  → mammoth.extractRawText
txt   → Buffer.toString('utf-8')
md    → Buffer.toString('utf-8')
```

在分片上传链路中，解析入口是：

> `server/api/upload/merge.post.js`

也就是所有分片上传完成后，服务端先合并完整文件，再读取完整文件 Buffer，然后调用 `parseDocument(fileBuffer, session.fileName)` 提取文本。

核心解析逻辑集中在 `documentParser.js` 中，PDF 用 `pdf-parse`，DOCX 用 `mammoth`，TXT 和 MD 直接转字符串。

#### 1. 为什么要做文件解析？

RAG 系统不能直接处理原始文件。

比如用户上传的是：

```
前端面试资料.pdf
Vue笔记.docx
项目总结.md
```

但是后续的 RAG 流程需要的是：

```
纯文本 text
```

因为后面要做：

```
文本分块
  ↓
生成 embedding
  ↓
存入向量库
  ↓
用户提问时语义检索
```

所以文件解析组件的作用就是：

```
原始文件 Buffer
  ↓
parseDocument
  ↓
纯文本 text
```

可以这样理解：

> 文件解析是 RAG 入库链路中的“格式转换层”，负责把不同格式的文件统一转换成可分块、可向量化的纯文本。

#### 2. PDF 解析原理

##### 当前项目怎么做？

当前项目里 PDF 解析是：

```js
const data = await pdf(Buffer.from(buffer))return data.text || ''
```

也就是使用 `pdf-parse` 从 PDF Buffer 中提取 `data.text`。

##### PDF 解析本质是什么？

PDF 本质上不是像 HTML 那样的结构化文档。

它更像是一个“排版结果文件”。

里面通常存的是：

```
文字内容
字体信息
坐标位置
页面信息
图片资源
绘制指令
```

`pdf-parse` 做的事情大概是：

```
读取 PDF Buffer
  ↓
解析 PDF 内部对象
  ↓
找到文本对象
  ↓
按照页面顺序提取文字
  ↓
拼成 data.text
```

所以它适合解析 **文本型 PDF**。

比如：

```
电子书 PDF
论文 PDF
导出的简历 PDF
文档转出的 PDF
```

这些 PDF 内部本身有文字层，所以可以提取出来。

##### PDF 解析的局限

如果用户上传的是扫描件 PDF：

```
扫描版合同
拍照生成的 PDF
图片版论文
```

这种 PDF 里面看起来有字，但本质上是图片。

这时 `pdf-parse` 很可能提取不到文本。

也就是说：

```
文本型 PDF  → 可以提取
扫描型 PDF  → 需要 OCR
```

面试里可以主动说：

> 当前项目的 PDF 解析依赖 PDF 本身存在文字层，所以对文本型 PDF 支持较好。如果是扫描件 PDF，后续需要接 OCR，把图片中的文字识别出来后再进入分块和向量化流程。

#### 3. DOCX 解析原理

##### 当前项目怎么做？

当前项目里 DOCX 解析是：

```js
const result = await mammoth.extractRawText({
  buffer: Buffer.from(buffer)
})
return result.value || ''
```

也就是用 `mammoth.extractRawText` 提取 Word 文档中的原始文本。

##### DOCX 本质是什么？

DOCX 不是一个普通文本文件。

它本质上是一个压缩包，里面有很多 XML 文件。

可以理解成：

```
example.docx
  ├─ word/document.xml
  ├─ word/styles.xml
  ├─ word/media/*
  ├─ docProps/*
  └─ ...
```

真正的正文内容通常在：

```
word/document.xml
```

`mammoth` 做的事情是：

```
读取 DOCX Buffer
  ↓
解压内部 XML
  ↓
读取正文段落
  ↓
提取文本内容
  ↓
返回 result.value
```

当前项目用的是 `extractRawText`，所以目标是：

```
尽快拿到纯文本
```

而不是完整还原 Word 样式。

##### DOCX 解析的局限

当前这种方式不会精细保留：

```
标题层级
加粗 / 斜体
表格结构
图片
页眉页脚
批注
复杂编号结构
```

所以如果 DOCX 里有表格：

```
| 参数 | 含义 |
| ---- | ---- |
| topK | 检索条数 |
```

直接提取纯文本后，可能会变成：

```
参数 含义 topK 检索条数
```

结构信息会丢失。

面试里可以说：

> 当前 DOCX 解析使用 `mammoth.extractRawText`，优点是简单稳定，能快速拿到正文文本。但它偏纯文本提取，表格、标题层级、图片等结构化信息会丢失。后续如果要提升 RAG 效果，可以考虑把 DOCX 解析成带层级结构的 Markdown，再进入分块流程。

#### 4. TXT / Markdown 解析原理

##### 当前项目怎么做？

当前项目里 TXT 和 MD 是：

```js
return Buffer.from(buffer).toString('utf-8')
```

也就是直接把 Buffer 按 UTF-8 转字符串。

##### 为什么 TXT / MD 可以直接转？

因为 TXT 和 Markdown 本身就是文本文件。

不像 PDF / DOCX 那样需要解析复杂格式。

所以处理链路是：

```
Buffer
  ↓
toString('utf-8')
  ↓
文本内容
```

##### Markdown 当前的局限

当前项目把 Markdown 当普通文本处理。

这能用，但没有充分利用 Markdown 的结构。

Markdown 里其实天然有层级：

```
# 一级标题

## 二级标题

- 列表项

```js
const a = 1
```

```

如果只是普通字符串读取，后续分块时可能不会特别照顾：

```text
标题和正文的关系
代码块完整性
列表完整性
引用块完整性
```

更好的方式是：

```
Markdown
  ↓
解析成 AST
  ↓
按标题层级组织内容
  ↓
转成带结构的文本块
```

比如一个 chunk 可以带上标题上下文：

```
标题路径：RAG 系统 / 文本分块 / overlap
内容：overlap 的作用是减少语义断裂...
```

这样检索效果通常更好。

### 优化

#### 不要只靠文件后缀判断类型

当前解析分发主要依赖：

```js
const ext = filename.split('.').pop().toLowerCase()
```

问题是文件后缀可以伪造。

比如用户把一个图片改名成：

```
test.pdf
```

后端会认为它是 PDF，然后尝试解析，最终可能报错。

更好的方式是增加 **文件魔数校验**。

比如：

```
PDF 文件通常以 %PDF 开头
DOCX 本质是 zip，通常以 PK 开头
```

优化后可以变成：

```
先看后缀
  ↓
再看 MIME / magic number
  ↓
两者一致才进入解析
```

面试可以这样说：

> 当前项目主要通过文件后缀判断类型，这对个人项目够用，但生产环境不够严谨。后续可以增加 magic number 校验，防止用户伪造后缀导致解析异常或安全风险。

#### 增加文本清洗 normalize

当前 `parseDocument` 返回的是原始提取文本。

但是 PDF / DOCX 提取出来的文本可能会有很多问题：

```
多余空行
页眉页脚
页码
断行
连续空格
不可见字符
乱码字符
```

可以在解析后统一做一层清洗：

```js
function normalizeText(text) {
  return text
    .replace(/\r\n/g, '\n')
    .replace(/\n{3,}/g, '\n\n')
    .replace(/[ \t]{2,}/g, ' ')
    .trim()
}
```

这样后续分块质量会更好。

面试可以说：

> 文件解析不只是把文本拿出来，还要尽量保证文本质量。后续我会在解析后增加 normalizeText，比如统一换行、去除多余空白、清理页码和页眉页脚，减少无效内容进入向量库。

#### PDF 按页解析并保留页码

当前 PDF 解析返回的是一个整体字符串。

问题是：

```
后续召回 chunk 时，无法知道它来自 PDF 第几页
```

更好的做法是保留 metadata：

```json
{
  text: 'Vue3 使用 Proxy 实现响应式...',
  metadata: {
    fileName: 'Vue面试.pdf',
    page: 12
  }
}
```

这样问答时可以展示来源：

```
参考来源：Vue面试.pdf 第 12 页
```

这对 RAG 项目很加分。

面试可以说：

> 当前项目解析后只返回纯文本，metadata 比较少。后续可以把 PDF 按页解析，并把 pageNumber 写入 chunk metadata。这样检索结果不仅能回答，还能展示来源页码，提升可解释性和可信度。

#### Markdown 结构化解析

当前 Markdown 是普通字符串读取。

可以优化成：

```
Markdown
  ↓
marked / markdown-it 解析
  ↓
识别标题、列表、代码块、引用
  ↓
按标题层级组织 chunk
```

比如：

```
# Vue
## 响应式
Vue3 使用 Proxy...
```

可以转换成：

```
{
  titlePath: ['Vue', '响应式'],
  content: 'Vue3 使用 Proxy...',
}
```

好处是：

```
chunk 语义更完整
标题上下文不容易丢
检索结果更准确
```

面试可以说：

> Markdown 本身有天然结构，后续可以不再简单 `toString`，而是解析 Markdown AST，按标题层级组织 chunk，并把标题路径作为 metadata 或拼入 chunk 内容，提高召回质量。

#### DOCX 表格结构处理

当前 DOCX 用 `extractRawText`，会丢失表格结构。

如果文档里有大量表格，比如：

```
接口字段说明
配置参数说明
实验结果对比
```

纯文本提取后效果可能不好。

可以优化成：

```
DOCX
  ↓
提取段落
  ↓
提取表格
  ↓
表格转 Markdown table
  ↓
再进入分块
```

例如把表格转成：

```
| 参数 | 含义 |
| --- | --- |
| topK | 检索召回条数 |
| temperature | 模型随机性 |
```

这样向量化后的语义更清楚。

#### 扫描版 PDF 接 OCR

当前 `pdf-parse` 只能处理文本层。

如果遇到扫描件，需要 OCR。

优化链路可以是：

```
pdf-parse 提取文本
  ↓
如果 text 为空或过短
  ↓
判断是否扫描件
  ↓
将页面转图片
  ↓
调用 OCR
  ↓
得到文本
```

可以用：

```
Tesseract
PaddleOCR
云 OCR 服务
```

面试可以说：

> 当前项目对文本型 PDF 支持较好，但扫描件 PDF 无法解析。后续可以增加 OCR fallback：如果 `pdf-parse` 提取文本为空，就将 PDF 页面转成图片，再通过 OCR 提取文字。

#### 解析任务异步化

当前 `/api/upload/merge` 做了：

```
合并文件
  ↓
解析文件
  ↓
返回 text
```

如果文件变大，解析时间会变长，请求可能阻塞。

可以优化成异步任务：

```
上传完成
  ↓
创建 parsing job
  ↓
立即返回 documentId
  ↓
服务端后台解析
  ↓
前端轮询 / SSE 监听状态
  ↓
解析完成后继续分块和向量化
```

文档状态可以变成：

```
uploaded
parsing
parsed
chunking
embedding
indexed
failed
```

面试可以说：

> 当前项目是同步解析，个人项目没问题。但如果文件更大或并发更多，我会把解析改成异步任务，前端只展示处理状态，服务端后台完成解析、分块和向量化，避免单个请求耗时过长。

#### 解析缓存

同一个 `fileHash` 的文件如果已经解析过，就没必要再次解析。

当前项目已经通过 `fileHash` 做秒传。

可以进一步把解析结果也缓存：

```
fileHash
  ↓
parsedText / parsedBlocks
```

下次同文件上传时：

```
命中 fileHash
  ↓
直接复用解析结果和向量数据
```

当前秒传已经有类似思路，但可以把缓存设计得更明确。

### 问答
#### 文件解析在 RAG 链路中处于什么位置？

可以答：

> 文件解析位于上传之后、文本分块之前。用户上传的是 PDF、DOCX、TXT、MD 等原始文件，但 RAG 后续需要处理的是纯文本。所以服务端会先把文件解析成 text，然后前端再把 text 交给 Web Worker 做分块，最后送到 embedding 接口生成向量。

---

#### 为什么要在服务端解析，而不是前端解析？

可以答：

> 服务端解析更稳定，也更安全。PDF、DOCX 这类格式解析依赖 Node 生态库，比如 `pdf-parse` 和 `mammoth`，放在服务端更方便处理 Buffer、文件存储和异常。
> 
> 另外，服务端解析可以统一控制文件大小、文件类型和安全校验，避免把复杂解析逻辑和大文件处理压力放到浏览器端。前端只负责上传和展示处理状态。

#### PDF 解析为什么有时候提取不到文字？

可以答：

> 因为有些 PDF 是扫描件，里面没有真实文字层，本质上只是图片。`pdf-parse` 只能提取 PDF 中已有的文本对象，不能识别图片里的文字。所以扫描版 PDF 需要接 OCR，把页面图片转成文本后再进入分块流程。

#### DOCX 解析为什么用 mammoth？

可以答：

> DOCX 本质是一个压缩包，正文在内部 XML 中。`mammoth` 可以读取 DOCX Buffer 并提取正文文本。当前项目用的是 `extractRawText`，目标是快速拿到纯文本，供后续分块和向量化使用。
> 
> 不过它不会完整保留样式、表格、图片等结构，所以如果文档里表格很多，后续可以把 DOCX 转成 Markdown 或结构化 blocks。

#### Markdown 为什么直接 `toString`，这样好吗？

可以答：

> 当前直接 `Buffer.toString('utf-8')` 是因为 Markdown 本身就是文本格式，这样实现简单，能快速进入分块流程。
> 
> 但从 RAG 效果看，Markdown 可以优化。因为 Markdown 有标题、列表、代码块等结构，后续可以解析 Markdown AST，按标题层级切分，并把 headingPath 作为 metadata，这样 chunk 的语义会更完整。

#### 解析后的文本为空怎么办？

可以答：

> 当前项目在 `merge.post.js` 中会校验 `text?.trim()`，如果为空就抛出“未能从文件中提取到文本内容”。这样可以避免空文本继续进入分块和向量化流程。
> 
> 后续可以把错误信息做得更细，比如区分“扫描件 PDF 需要 OCR”“文件损坏”“编码不支持”“文档为空”等，让用户知道具体原因。

#### 解析结果为什么不能直接拿去问大模型？

可以答：

> 因为文档可能很长，直接把全文塞给大模型会超过上下文长度，也会浪费 token。RAG 的做法是先把解析出来的文本切成多个 chunk，然后对 chunk 做 embedding 存入向量库。用户提问时，只召回相关 chunk，再把相关上下文交给大模型回答。

#### 解析和分块有什么区别？

可以答：

> 解析是把不同格式的文件统一转成文本，比如 PDF 转 text、DOCX 转 text。
> 
> 分块是把解析后的长文本切成多个小文本片段，比如按段落、句子、固定长度切分，并设置 overlap。
> 
> 所以解析解决的是“文件格式转换”问题，分块解决的是“长文本如何适配向量检索和模型上下文”问题。

#### 如果一个 PDF 有页眉页脚，会影响 RAG 吗？

可以答：

> 会影响。页眉、页脚、页码这类内容如果反复出现在每一页，会进入很多 chunk，导致向量库里出现大量噪声。用户检索时可能召回无效内容。
> 
> 所以后续可以在解析后增加文本清洗，比如识别重复页眉页脚、页码、连续空行和特殊字符，提升 chunk 质量。

#### 怎么提升解析后的检索效果？

可以答：

> 可以从三个方向优化。
> 
> 第一是保留结构，比如 PDF 保留页码，Markdown 保留标题路径，DOCX 表格转 Markdown table。
> 
> 第二是做文本清洗，比如去掉页眉页脚、空白字符、乱码和重复内容。
> 
> 第三是增强 metadata，比如每个 chunk 记录 documentId、fileName、pageNumber、headingPath、chunkIndex。这样检索结果不仅能召回内容，还能展示来源。

#### 文件类型校验只看后缀安全吗？

可以答：

> 不够安全。后缀可以伪造，比如把图片改成 `.pdf`。当前项目对个人项目来说够用，但生产环境应该增加 MIME 或 magic number 校验。比如 PDF 通常以 `%PDF` 开头，DOCX 本质是 zip，通常以 `PK` 开头。这样可以避免伪造后缀导致解析异常或安全问题。

#### 如果要支持更多格式怎么扩展？

可以答：

> 当前 `documentParser.js` 是根据 ext 做 switch 分发。要支持新格式，可以继续增加 case，比如 `html`、`csv`、`xlsx`、`pptx`。
> 
> 如果格式越来越多，可以把 switch 优化成策略映射，比如：
> 
> ```
> const parsers = {  pdf: parsePdf,  docx: parseDocx,  md: parseMarkdown,  txt: parseTxt}
> ```
> 
> 这样新增格式只需要注册新的 parser，结构会更清晰。


## 3. 文本分块组件

```
文本分块组件
  ├─ 调度层：app/composables/useDocuments.js
  │   ├─ chunkTextWithWorker
  │   └─ fallbackChunkAdvanced
  │
  ├─ 执行层：public/workers/textProcessor.worker.js
  │   ├─ self.onmessage
  │   ├─ recursiveChunk
  │   ├─ chunkBySentences
  │   └─ chunkByFixedSize
  │
  └─ 配置来源：/api/settings
      ├─ chunkSize
      └─ chunkOverlap
```

文本分块这一块没有独立页面组件，它属于 **文档上传后的文本处理模块**。当前主要由两个文件承担：

```
app/composables/useDocuments.js
public/workers/textProcessor.worker.js
```

前者负责调度，后者负责真正分块。

### 3.1 文本分块调度模块

>文件：`app/composables/useDocuments.js`

- 位于前端业务编排层
- 在文件解析完成后接收服务端返回的 `result.text`
- 请求 `/api/settings` 获取分块配置
- 调用 `chunkTextWithWorker`
- 把长文本传给 Web Worker
- 接收 Worker 返回的 `chunks`
- 把 `chunks` 继续传给 `/api/embed` 做向量化
- 在 Worker 不可用时，使用降级分块函数

==作用==：这个模块不是具体执行分块算法的地方，而是 **文本分块流程的调度层**。它负责决定什么时候开始分块、使用什么分块参数、分块完成后把结果交给谁。当前代码里是在上传合并并解析出 `text` 后，读取 `settings.chunkSize` 和 `settings.chunkOverlap`，然后调用 Worker 处理文本。

### 3.2 Web Worker 分块执行组件

>文件：`public/workers/textProcessor.worker.js`

- 位于浏览器 Worker 线程
- 接收主线程传来的文本和配置
- 根据 `type: 'chunk'` 判断任务类型
- 调用 `recursiveChunk` 执行递归分块
- 分块成功后返回 `{ type: 'chunks', payload: chunks }`
- 分块失败后返回 `{ type: 'error', payload: error.message }`
- 避免大文本处理阻塞页面主线程

==作用==：这是文本分块真正执行的地方。因为解析后的文档文本可能比较长，如果直接在主线程切分，会影响页面交互，所以项目把这部分逻辑放到了 Web Worker 中。Worker 接收文本后，在后台线程完成段落、句子、固定长度的递归切分，再把结果返回给主线程。

### 3.2 递归分块策略模块

>文件：`public/workers/textProcessor.worker.js`

- 负责文本分块的主算法
- 先清理连续空行
- 优先按段落切分
- 如果段落过长，再按句子切分
- 如果句子仍然过长，最后按固定长度切分
- 支持 `chunkOverlap`
- 返回最终的文本块数组

==作用==：这是项目里最核心的分块策略。它不是简单地每隔固定长度截断，而是优先保证语义完整：能按段落切就按段落切，段落太长再按句子，句子还太长才用固定长度兜底。这样做比纯固定长度切分更适合 RAG，因为 chunk 的语义边界更自然。

### 3.4 句子级分块模块

>文件：`public/workers/textProcessor.worker.js`

- 负责处理超长段落
- 根据中文和英文句末符号切句
- 支持 `。！？.!?` 等标点
- 尽量把多个句子合并到一个 chunk 中
- 如果单个句子仍然超过 `chunkSize`，继续交给固定长度分块兜底

==作用==：这个模块是递归分块里的第二层策略。它解决的是“某个段落太长，但直接固定长度切又容易切断语义”的问题。通过句子级切分，可以让每个 chunk 尽量保留完整句子。

### 3.5 固定长度兜底分块模块

文件：`public/workers/textProcessor.worker.js`

- 负责最终兜底切分
- 按固定 `size` 截取文本
- 支持 `overlap`
- 使用 `step = size - overlap` 控制滑动窗口
- 保证无论文本多长，都能被切成可处理的 chunk

==作用==：这是最后的保底策略。当前面的段落切分、句子切分都无法处理超长文本时，就用固定长度强制切开。它保证分块流程一定能产出结果，不会因为单段或单句过长导致后续 embedding 无法继续。

### 3.6 分块参数来源模块

文件：服务端设置接口，前端通过 `$fetch('/api/settings')` 获取

- 提供 `chunkSize`
- 提供 `chunkOverlap`
- 支持用户在设置页调整分块参数
- 让分块策略不是写死在 Worker 里
- 上传时读取最新配置后再传给 Worker

==作用==：它是文本分块的配置来源。当前分块大小和重叠长度不是完全写死，而是上传流程中动态读取设置，再传给 Worker。这样用户可以根据文档类型调整分块策略，比如长文档可以调大 `chunkSize`，需要更多上下文连续性时可以调大 `chunkOverlap`。

### 3.7 Worker 降级分块模块

文件：`app/composables/useDocuments.js`

- 当浏览器不支持 Worker 或 Worker 初始化失败时使用
- 在主线程按固定长度简单切分
- 不支持复杂的段落、句子递归策略
- 保证极端情况下流程仍然可以继续

==作用==：这是容错模块。正常情况下项目会优先使用 Worker 分块，但如果 Worker 不可用，就退回到主线程固定长度分块。它的效果没有 Worker 里的递归分块好，但可以保证上传入库流程不中断。

### 面试里回答

> 文本分块模块主要由 `useDocuments.js` 和 `textProcessor.worker.js` 组成。`useDocuments.js` 负责在文件解析出 text 后读取分块配置，并调用 `chunkTextWithWorker` 把文本交给 Worker；`textProcessor.worker.js` 负责真正执行分块算法。它采用递归策略，优先按段落切分，段落过长再按句子切分，句子仍然过长时按固定长度兜底，并通过 overlap 保留相邻 chunk 的上下文连续性。